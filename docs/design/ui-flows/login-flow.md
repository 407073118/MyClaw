# 登录流程

## 1. 目标

描述当前仓库中“桌面端登录到云端”的根层流程，帮助 Agent 理解跨工作区协作链路。

## 2. 流程

1. 用户在桌面端发起登录。
2. 桌面端调用云端认证接口。
3. 云端完成账号校验并返回会话信息。
4. 桌面端保存会话并进入后续 Hub 浏览、安装或同步流程。

## 3. 边界

- 桌面端 UI 与本地状态逻辑归 `desktop/`。
- 云端认证与会话逻辑归 `cloud/`。
- 根层只描述跨工作区流程，不描述具体接口实现。

