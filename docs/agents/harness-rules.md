# Harness Rules

## Agent 必须做的事

- 先判断任务落点，再进入对应层级。
- 优先遵守最近一层 `AGENTS.md`。
- 修改中文文件时先读、再改、再复读。
- 修改契约时同步更新文档、调用端、测试。

## Agent 不应做的事

- 不在根层替代局部工作区做详细实现设计。
- 不把未来式猜想写成现状说明。
- 不在不确定编码状态下重写整份中文文件。
- 不跨工作区复制粘贴局部规则。

## 推荐行为

- 用目录边界控制上下文。
- 用最小改动控制风险。
- 用计划和明确写入范围支持 subagents 并行开发。

