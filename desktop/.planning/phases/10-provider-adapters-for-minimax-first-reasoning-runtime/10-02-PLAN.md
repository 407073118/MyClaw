---
phase: 10-provider-adapters-for-minimax-first-reasoning-runtime
plan: 02
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - src/main/services/model-client.ts
  - src/main/ipc/sessions.ts
  - src/main/services/reasoning-runtime.ts
  - tests/phase10-message-replay.test.ts
  - tests/phase10-minimax-adapter.test.ts
autonomous: true
requirements:
  - P10-03
  - P10-04
user_setup: []
must_haves:
  truths:
    - transport 会输出可 replay 的完整 assistant turn，而不是只输出拆平文本
    - sessions/tool loop 使用 replay payload 保持 reasoning continuity
    - 发生 provider 不兼容时会自动降级并留下可解释日志
  artifacts:
    - desktop/src/main/services/model-client.ts
    - desktop/src/main/ipc/sessions.ts
    - desktop/tests/phase10-message-replay.test.ts
  key_links:
    - model-client 规范化结果直接服务于 sessions 的 replay 行为
    - reasoning runtime 的 replay policy 被 sessions/tool loop 真正消费
---

<objective>
把 MiniMax 的完整 assistant/reasoning/tool 回放语义真正接进 transport 和 tool loop，解决 “能显示 reasoning 但多轮越跑越钝” 的问题。

Purpose: 让 MiniMax-first 适配从“请求参数更像官方推荐”升级为“多轮 agent loop 语义真正稳定”。
Output: replay-capable transport 结果、sessions/tool loop replay 集成、降级日志与自动化验证。
</objective>

<execution_context>
@/Users/zhangjianing/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhangjianing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-provider-adapters-for-minimax-first-reasoning-runtime/10-CONTEXT.md
@.planning/phases/10-provider-adapters-for-minimax-first-reasoning-runtime/10-RESEARCH.md
@.planning/phases/10-provider-adapters-for-minimax-first-reasoning-runtime/10-01-PLAN.md
@src/main/services/model-client.ts
@src/main/ipc/sessions.ts
@src/main/services/reasoning-runtime.ts
@/Users/zhangjianing/WebstormProjects/claude-code/src/utils/messages.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 transport 结果为 replay-capable assistant payload</name>
  <files>src/main/services/model-client.ts, src/main/services/reasoning-runtime.ts</files>
  <read_first>src/main/services/model-client.ts, src/main/services/reasoning-runtime.ts, src/main/services/provider-adapters/minimax.ts, .planning/phases/10-provider-adapters-for-minimax-first-reasoning-runtime/10-RESEARCH.md</read_first>
  <action>扩展 `model-client.ts` 的规范化结果，让它除了 `content/reasoning/toolCalls` 外，还能返回供后续历史回放使用的完整 assistant replay payload。该 payload 至少要能保留：完整 assistant 消息结构、tool calls、reasoning/thinking 内容，以及 provider adapter 需要的额外 replay hints。同步让 `reasoning-runtime.ts` 明确 replay policy 与 provider adapter 的关联。必须补中文注释和中文日志，说明当前返回的是哪种 replay payload，以及是否退化为兼容回放。</action>
  <verify>rg -n "replay|assistant payload|replayPolicy|toolCalls" desktop/src/main/services/model-client.ts desktop/src/main/services/reasoning-runtime.ts</verify>
  <acceptance_criteria>
    - `model-client.ts` 返回值包含 replay-capable payload
    - replay payload 至少保留 assistant message、reasoning、tool_calls 所需信息
    - `reasoning-runtime.ts` 能表达 replay policy 与降级原因
  </acceptance_criteria>
  <done>transport 已经可以把 MiniMax 响应变成后续 tool loop 可消费的 replay payload。</done>
</task>

<task type="auto">
  <name>Task 2: 接入 sessions/tool loop replay 与自动降级测试</name>
  <files>src/main/ipc/sessions.ts, tests/phase10-message-replay.test.ts, tests/phase10-minimax-adapter.test.ts</files>
  <read_first>src/main/ipc/sessions.ts, src/main/services/model-client.ts, tests/phase9-thinking-mode.test.ts, tests/phase9-provider-reasoning-mapper.test.ts, .planning/phases/10-provider-adapters-for-minimax-first-reasoning-runtime/10-VALIDATION.md</read_first>
  <action>更新 `sessions.ts` 的 agentic loop，让 tool use 多轮会话回放完整 assistant payload，而不是只回放拆平字段。需要覆盖至少两条路径：1）MiniMax enhanced/official-friendly replay；2）发生 provider 不兼容或 adapter 判定失败时自动降级到兼容 replay，同时保留 `degradedReason`。新增 `tests/phase10-message-replay.test.ts`，覆盖完整 assistant payload 回放、tool result 续写和自动降级；必要时补充 `phase10-minimax-adapter.test.ts` 的 runtime/sessions 断言。不要改变既有 Chat UI 的外部行为，只修复运行时稳定性。</action>
  <verify>pnpm --dir desktop exec vitest run tests/phase10-message-replay.test.ts tests/phase10-minimax-adapter.test.ts</verify>
  <acceptance_criteria>
    - `desktop/tests/phase10-message-replay.test.ts` exists
    - sessions/tool loop 使用 replay payload，而不是只依赖 `content` 字符串
    - 自动降级路径能输出明确 degraded reason
    - 原有 agentic loop ceiling / approval / tool execution 逻辑不被破坏
  </acceptance_criteria>
  <done>MiniMax 多轮 tool loop 已具备更完整的 reasoning continuity，并有自动化回归保护。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --dir desktop exec vitest run tests/phase10-message-replay.test.ts tests/phase10-minimax-adapter.test.ts`
- [ ] `rg -n "replay|degradedReason" desktop/src/main/ipc/sessions.ts desktop/src/main/services/model-client.ts`
- [ ] tool loop 仍保留原有审批、并发与 safety ceiling 逻辑
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- tool loop 对 MiniMax 的 assistant / tool / reasoning 回放语义已稳定
  </success_criteria>

<output>
After completion, create `.planning/phases/10-provider-adapters-for-minimax-first-reasoning-runtime/10-02-SUMMARY.md`
</output>
