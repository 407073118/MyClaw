---
phase: 10-provider-adapters-for-minimax-first-reasoning-runtime
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/services/reasoning-runtime.ts
  - src/main/services/model-capability-resolver.ts
  - src/main/services/model-capability-registry.ts
  - src/main/ipc/models.ts
  - src/main/services/provider-adapters/index.ts
  - src/main/services/provider-adapters/minimax.ts
  - tests/phase10-model-capability-resolver.test.ts
  - tests/phase10-minimax-adapter.test.ts
autonomous: true
requirements:
  - P10-01
  - P10-02
user_setup: []
must_haves:
  truths:
    - MiniMax 有 first-class capability profile，而不是散落的 minimax 特判
    - runtime 可以通过 adapter contract 选择兼容模式或增强模式
    - 旧的 MiniMax requestBody / endpoint 使用方式继续可用
  artifacts:
    - desktop/src/main/services/provider-adapters/minimax.ts
    - desktop/src/main/services/provider-adapters/index.ts
    - desktop/tests/phase10-minimax-adapter.test.ts
  key_links:
    - reasoning runtime 调用 adapter 生成 request patch 与 replay policy
    - models IPC / capability registry / resolver 对 MiniMax 语义一致
---

<objective>
建立 Phase 10 的 adapter contract 和 MiniMax capability profile，让 MiniMax 从分散特判升级为正式 provider adapter。

Purpose: 把 Phase 9 的 provider-neutral runtime 接到真正的 provider 语义上，同时守住“现有调用方式不能坏”这一硬约束。
Output: adapter contract、MiniMax adapter、capability profile、models IPC / registry / resolver 对齐，以及对应合同测试。
</objective>

<execution_context>
@/Users/zhangjianing/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhangjianing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-provider-adapters-for-minimax-first-reasoning-runtime/10-CONTEXT.md
@.planning/phases/10-provider-adapters-for-minimax-first-reasoning-runtime/10-RESEARCH.md
@../docs/plans/2026-04-04-minimax-reasoning-runtime-design.md
@src/main/services/reasoning-runtime.ts
@src/main/services/model-capability-resolver.ts
@src/main/services/model-capability-registry.ts
@src/main/ipc/models.ts
@src/renderer/pages/ModelDetailPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立 provider adapter contract 与 MiniMax adapter</name>
  <files>src/main/services/provider-adapters/index.ts, src/main/services/provider-adapters/minimax.ts, src/main/services/reasoning-runtime.ts</files>
  <read_first>src/main/services/reasoning-runtime.ts, src/main/services/model-client.ts, .planning/phases/10-provider-adapters-for-minimax-first-reasoning-runtime/10-CONTEXT.md, .planning/phases/10-provider-adapters-for-minimax-first-reasoning-runtime/10-RESEARCH.md, /Users/zhangjianing/WebstormProjects/claude-code/src/query.ts</read_first>
  <action>新增统一 provider adapter contract，例如暴露 `buildRequestPatch()`、`selectMode()`、`resolveReplayPolicy()`、`normalizeProviderHints()` 等能力；新建 `minimax.ts` 作为第一个 first-class adapter，明确区分 compatibility mode 与 enhanced mode。把 `reasoning-runtime.ts` 从 “只会返回 openai-compatible patch” 升级为 “先解析 capability / provider，再委托 adapter 产出执行计划”。严格保持用户现有调用方式继续可用：兼容模式下不得强制切换 endpoint、不得覆盖用户自定义 `profile.requestBody`。所有新增方法写中文注释和中文日志，日志至少要能说明当前选择了哪种 MiniMax mode，以及为什么发生降级。</action>
  <verify>rg -n "selectMode|buildRequestPatch|compatibility|enhanced|degradedReason" desktop/src/main/services/provider-adapters desktop/src/main/services/reasoning-runtime.ts</verify>
  <acceptance_criteria>
    - `desktop/src/main/services/provider-adapters/minimax.ts` exists
    - adapter contract 明确区分 compatibility / enhanced mode
    - `desktop/src/main/services/reasoning-runtime.ts` 通过 adapter 产出 execution plan
    - 代码中保留 `profile.requestBody` 最终覆盖权
  </acceptance_criteria>
  <done>MiniMax 已有正式 adapter，runtime 能基于 capability 选择兼容或增强模式。</done>
</task>

<task type="auto">
  <name>Task 2: 补齐 MiniMax capability profile、provider flavor 与合同测试</name>
  <files>src/main/services/model-capability-registry.ts, src/main/services/model-capability-resolver.ts, src/main/ipc/models.ts, tests/phase10-model-capability-resolver.test.ts, tests/phase10-minimax-adapter.test.ts</files>
  <read_first>src/main/services/model-capability-registry.ts, src/main/services/model-capability-resolver.ts, src/main/ipc/models.ts, tests/phase10-model-capability-resolver.test.ts, .planning/phases/10-provider-adapters-for-minimax-first-reasoning-runtime/10-VALIDATION.md</read_first>
  <action>为 MiniMax 增加显式 capability profile，至少表达：supportsReasoning、supportsEffort、requiresReasoningReplay、preferredProtocol，以及 compatibility/enhanced mode 需要的原始提示字段。同步整理 `ipc/models.ts` 的 provider flavor 推断和 catalog 归一化，避免设置页、catalog、runtime 三处对 MiniMax 语义不一致。扩展 `phase10-model-capability-resolver.test.ts` 并新增 `phase10-minimax-adapter.test.ts`，覆盖：1）MiniMax capability 命中；2）未显式配置 providerFlavor 时的自动识别；3）compatibility mode 不破坏旧接法；4）enhanced mode 在 capability 满足时产生更强 patch；5）不支持场景给出明确 degraded reason。</action>
  <verify>pnpm --dir desktop exec vitest run tests/phase10-model-capability-resolver.test.ts tests/phase10-minimax-adapter.test.ts</verify>
  <acceptance_criteria>
    - registry/resolver 对 MiniMax 有显式能力语义
    - `ipc/models.ts` 对 MiniMax 的 flavor 推断、catalog 路径与 runtime 语义一致
    - `desktop/tests/phase10-minimax-adapter.test.ts` exists
    - 测试覆盖 compatibility mode、enhanced mode、degraded reason
  </acceptance_criteria>
  <done>MiniMax capability profile、adapter mode 与 models IPC 已经统一并具备回归保护。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --dir desktop exec vitest run tests/phase10-model-capability-resolver.test.ts tests/phase10-minimax-adapter.test.ts`
- [ ] `rg -n "minimax|compatibility|enhanced" desktop/src/main/services/provider-adapters desktop/src/main/ipc/models.ts`
- [ ] 兼容模式仍保留旧 requestBody / endpoint 使用方式
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- MiniMax 已成为 first-class capability + adapter，而不是散落特判
  </success_criteria>

<output>
After completion, create `.planning/phases/10-provider-adapters-for-minimax-first-reasoning-runtime/10-01-SUMMARY.md`
</output>
