---
phase: 09-chat-thinking-effort
plan: 02
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - src/main/ipc/sessions.ts
  - src/main/services/model-client.ts
  - src/preload/index.ts
  - src/renderer/types/electron.d.ts
  - tests/phase9-thinking-mode.test.ts
  - tests/phase9-provider-reasoning-mapper.test.ts
autonomous: true
requirements:
  - P9-02
  - P9-03
user_setup: []
must_haves:
  truths:
    - `callModel()` 只通过单点 mapper 组装 reasoning patch
    - unsupported provider 不发送未知 thinking/effort 参数
    - session send-message 链路可以携带会话 thinking 状态进入 main runtime
  artifacts:
    - desktop/src/main/services/model-client.ts
    - desktop/src/main/ipc/sessions.ts
    - desktop/tests/phase9-provider-reasoning-mapper.test.ts
  key_links:
    - sessions IPC 通过 runtime 把 thinking 状态交给 model-client
    - model-client 对 OpenAI-style patch 与 unsupported provider 空 patch 有稳定分支
---

<objective>
把 reasoning runtime 接到主进程消息发送和请求体组装链路中，形成可测试的 provider-aware patch 行为。

Purpose: 让 Phase 9 从“只有抽象”变成“真正能影响请求体、但又不破坏旧调用路径”的运行时能力。
Output: sessions/main/preload/electron 类型链路打通，request body mapper 可测。
</objective>

<execution_context>
@/Users/zhangjianing/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhangjianing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-chat-thinking-effort/09-CONTEXT.md
@.planning/phases/09-chat-thinking-effort/09-RESEARCH.md
@.planning/phases/09-chat-thinking-effort/09-01-PLAN.md
@src/main/ipc/sessions.ts
@src/main/services/model-client.ts
@src/preload/index.ts
@src/renderer/types/electron.d.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 打通会话到请求体的 reasoning runtime 链路</name>
  <files>src/main/ipc/sessions.ts, src/main/services/model-client.ts, src/preload/index.ts, src/renderer/types/electron.d.ts</files>
  <read_first>src/main/ipc/sessions.ts, src/main/services/model-client.ts, src/preload/index.ts, src/renderer/types/electron.d.ts, src/main/services/reasoning-runtime.ts, shared/contracts/session.ts</read_first>
  <action>在 `sessions.ts` 里把会话 thinking 状态纳入 `createSession` 默认值和 `send-message` 主链路；调用模型前必须先通过 `reasoning-runtime.ts` 生成本轮执行计划，再把 `bodyPatch` 交给 `model-client.ts`。更新 `model-client.ts`，新增单点请求 patch 组装逻辑，明确顺序为 `base request body -> reasoning body patch -> profile.requestBody`，确保用户已有 `requestBody` 仍能覆盖默认值。同步扩展 `preload/index.ts` 与 `renderer/types/electron.d.ts` 的 IPC 声明，让 renderer 可以创建或更新携带 thinking 状态的 session。必须在中文日志里记录 provider flavor、是否应用 patch、是否降级为空 patch。</action>
  <verify>rg -n "bodyPatch|thinkingEnabled|profile.requestBody|degradedReason" desktop/src/main/ipc/sessions.ts desktop/src/main/services/model-client.ts desktop/src/preload/index.ts desktop/src/renderer/types/electron.d.ts</verify>
  <acceptance_criteria>
    - `desktop/src/main/services/model-client.ts` contains a single reasoning patch merge point
    - `desktop/src/main/services/model-client.ts` merges `profile.requestBody` after reasoning patch
    - `desktop/src/main/ipc/sessions.ts` reads session `thinkingEnabled`
    - `desktop/src/preload/index.ts` and `desktop/src/renderer/types/electron.d.ts` expose the updated session API shape
  </acceptance_criteria>
  <done>thinking 状态可从 session 进入 request body，且现有 requestBody 覆盖优先级保持不变。</done>
</task>

<task type="auto">
  <name>Task 2: 为 reasoning mapper 与兼容降级补合同测试</name>
  <files>tests/phase9-thinking-mode.test.ts, tests/phase9-provider-reasoning-mapper.test.ts</files>
  <read_first>tests/phase12-token-budget-manager.test.ts, tests/phase13-context-assembly.test.ts, src/main/services/model-client.ts, src/main/services/reasoning-runtime.ts, .planning/phases/09-chat-thinking-effort/09-VALIDATION.md</read_first>
  <action>新增 `tests/phase9-thinking-mode.test.ts`，覆盖会话默认 thinking 状态、send-message 链路中的 runtime 调用与请求体透传；新增 `tests/phase9-provider-reasoning-mapper.test.ts`，至少覆盖两类分支：1）OpenAI-style / supported reasoning provider 返回 `reasoning: { effort: "medium" }` 之类的 patch；2）unsupported provider 返回空 patch，不把未知字段塞进请求体。测试命名和断言风格沿用现有 `desktop/tests` 约定，不使用 watch 模式。 </action>
  <verify>pnpm --dir desktop exec vitest run tests/phase9-thinking-mode.test.ts tests/phase9-provider-reasoning-mapper.test.ts</verify>
  <acceptance_criteria>
    - `desktop/tests/phase9-thinking-mode.test.ts` exists
    - `desktop/tests/phase9-provider-reasoning-mapper.test.ts` exists
    - test file contains `reasoning` patch assertions for supported provider
    - test file contains assertions that unsupported provider request body does not include reasoning fields
  </acceptance_criteria>
  <done>主进程 reasoning runtime 链路与兼容降级均有自动化回归保护。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --dir desktop exec vitest run tests/phase9-thinking-mode.test.ts tests/phase9-provider-reasoning-mapper.test.ts`
- [ ] `rg -n "profile.requestBody" desktop/src/main/services/model-client.ts`
- [ ] unsupported provider case proves empty patch behavior
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Request body mapper 已可稳定区分 supported / unsupported reasoning provider
  </success_criteria>

<output>
After completion, create `.planning/phases/09-chat-thinking-effort/09-02-SUMMARY.md`
</output>
