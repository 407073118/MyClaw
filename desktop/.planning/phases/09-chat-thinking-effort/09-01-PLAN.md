---
phase: 09-chat-thinking-effort
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/contracts/session.ts
  - shared/contracts/model.ts
  - src/main/services/model-capability-resolver.ts
  - src/main/services/state-persistence.ts
  - src/main/services/reasoning-runtime.ts
autonomous: true
requirements:
  - P9-01
user_setup: []
must_haves:
  truths:
    - ChatSession 持有抽象的 thinking 状态，并在会话创建、加载、保存后保持一致
    - 模型能力对象可以表达 reasoning 模式所需的最小语义，不要求 UI 了解 provider 细节
  artifacts:
    - desktop/src/main/services/reasoning-runtime.ts
    - desktop/shared/contracts/session.ts
    - desktop/shared/contracts/model.ts
  key_links:
    - 会话 thinking 状态可以从 shared contract 进入 main runtime
    - reasoning runtime 能读取 capability 并产出 provider-neutral 的计划结果
---

<objective>
建立 Phase 9 的基础契约和推理运行时骨架，让 thinking 状态与能力语义不再散落在调用链中。

Purpose: 先把抽象层做稳，避免后续在 IPC、UI、请求层重复解释同一件事。
Output: 会话契约、模型能力语义、reasoning runtime 骨架与持久化支持。
</objective>

<execution_context>
@/Users/zhangjianing/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhangjianing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-chat-thinking-effort/09-CONTEXT.md
@.planning/phases/09-chat-thinking-effort/09-RESEARCH.md
@../docs/plans/2026-04-04-minimax-reasoning-runtime-design.md
@shared/contracts/session.ts
@shared/contracts/model.ts
@src/main/services/model-capability-resolver.ts
@src/main/services/state-persistence.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展会话与模型能力契约</name>
  <files>shared/contracts/session.ts, shared/contracts/model.ts</files>
  <read_first>shared/contracts/session.ts, shared/contracts/model.ts, .planning/phases/09-chat-thinking-effort/09-CONTEXT.md, .planning/phases/09-chat-thinking-effort/09-RESEARCH.md</read_first>
  <action>在 `shared/contracts/session.ts` 为 `ChatSession` 新增抽象的 thinking 状态字段，建议使用 `thinkingEnabled?: boolean` 与可选 `thinkingSource?: "default" | "user-toggle"` 之类的产品语义字段；不要把 provider 原始参数名暴露到 session。同步在 `shared/contracts/model.ts` 为 `ModelCapability` 增加 reasoning 运行时需要的最小语义，例如 `supportsReasoning`、`supportsEffort`、`requiresReasoningReplay`、`preferredProtocol`，并保持旧字段兼容。必须补中文注释，明确“会话状态是产品抽象，provider 细节由 runtime 解释”。</action>
  <verify>rg -n "thinkingEnabled|thinkingSource|supportsEffort|requiresReasoningReplay|preferredProtocol" desktop/shared/contracts/session.ts desktop/shared/contracts/model.ts</verify>
  <acceptance_criteria>
    - `desktop/shared/contracts/session.ts` contains `thinkingEnabled`
    - `desktop/shared/contracts/model.ts` contains `supportsEffort`
    - `desktop/shared/contracts/model.ts` contains `requiresReasoningReplay`
    - 新增字段的注释明确说明“provider 细节不进入 session 层”
  </acceptance_criteria>
  <done>会话契约和能力契约都已能表达 reasoning runtime 所需状态。</done>
</task>

<task type="auto">
  <name>Task 2: 建立 reasoning runtime 骨架并接入持久化</name>
  <files>src/main/services/reasoning-runtime.ts, src/main/services/model-capability-resolver.ts, src/main/services/state-persistence.ts</files>
  <read_first>src/main/services/model-capability-resolver.ts, src/main/services/state-persistence.ts, shared/contracts/session.ts, shared/contracts/model.ts, .planning/phases/09-chat-thinking-effort/09-RESEARCH.md</read_first>
  <action>新建 `src/main/services/reasoning-runtime.ts`，导出 provider-neutral 的状态解释函数，例如 `resolveSessionThinkingState()`、`buildReasoningExecutionPlan()`。这一层只输出统一计划结果，例如 `enabled/disabled`、`bodyPatch`、`replayPolicy`、`degradedReason`，不要直接发送请求。同步更新 `model-capability-resolver.ts` 让新增 reasoning 语义字段能参与 effective capability 合并；更新 `state-persistence.ts`，确保新的 session thinking 字段会随会话保存与恢复。新增中文日志，至少覆盖“runtime 采用了哪种 thinking 状态”和“是否发生降级”。</action>
  <verify>rg -n "buildReasoningExecutionPlan|resolveSessionThinkingState|degradedReason|thinkingEnabled" desktop/src/main/services/reasoning-runtime.ts desktop/src/main/services/model-capability-resolver.ts desktop/src/main/services/state-persistence.ts</verify>
  <acceptance_criteria>
    - `desktop/src/main/services/reasoning-runtime.ts` contains `buildReasoningExecutionPlan`
    - `desktop/src/main/services/reasoning-runtime.ts` contains `degradedReason`
    - `desktop/src/main/services/state-persistence.ts` reads and writes `thinkingEnabled`
    - `desktop/src/main/services/model-capability-resolver.ts` merges new reasoning capability fields
  </acceptance_criteria>
  <done>reasoning runtime 骨架可独立解释状态，且 session 持久化不丢失 thinking 字段。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --dir desktop exec vitest run tests/phase9-thinking-mode.test.ts`
- [ ] `rg -n "thinkingEnabled|buildReasoningExecutionPlan" desktop/shared/contracts/session.ts desktop/src/main/services/reasoning-runtime.ts`
- [ ] session 保存/恢复路径包含新增字段
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Session 与 capability 层已具备 provider-neutral reasoning 语义
  </success_criteria>

<output>
After completion, create `.planning/phases/09-chat-thinking-effort/09-01-SUMMARY.md`
</output>
