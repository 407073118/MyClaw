---
phase: 09-chat-thinking-effort
plan: 03
type: execute
wave: 3
depends_on:
  - "09-01"
  - "09-02"
files_modified:
  - src/renderer/stores/workspace.ts
  - src/renderer/pages/ChatPage.tsx
  - tests/phase9-thinking-ui.test.ts
autonomous: true
requirements:
  - P9-04
user_setup: []
must_haves:
  truths:
    - ChatPage 提供轻量 thinking 开关与状态文案
    - 会话中途切换会给出确认，不悄悄改变后续运行风格
    - reasoning 展示仍然默认折叠，不压过正文
  artifacts:
    - desktop/src/renderer/pages/ChatPage.tsx
    - desktop/src/renderer/stores/workspace.ts
    - desktop/tests/phase9-thinking-ui.test.ts
  key_links:
    - renderer store 可以读写 session thinking 状态
    - ChatPage UI 与 session thinking 状态双向一致
---

<objective>
把 reasoning runtime 以克制的方式暴露到 Chat UI，让用户能主动控制 thinking，而不会把界面变成 provider 参数面板。

Purpose: 让 Phase 9 具备真实可感知的产品闭环，而不是只完成 main-process 内部改造。
Output: workspace 状态操作、ChatPage 轻量开关与确认交互、UI 回归测试。
</objective>

<execution_context>
@/Users/zhangjianing/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhangjianing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-chat-thinking-effort/09-CONTEXT.md
@.planning/phases/09-chat-thinking-effort/09-RESEARCH.md
@.planning/phases/09-chat-thinking-effort/09-02-PLAN.md
@src/renderer/stores/workspace.ts
@src/renderer/pages/ChatPage.tsx
@src/renderer/types/electron.d.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在 workspace 中接入会话级 thinking 状态</name>
  <files>src/renderer/stores/workspace.ts</files>
  <read_first>src/renderer/stores/workspace.ts, shared/contracts/session.ts, src/preload/index.ts, src/renderer/types/electron.d.ts</read_first>
  <action>扩展 `workspace.ts`，让创建会话、选择会话、发送消息后的 session 回写都能保留 `thinkingEnabled` 状态，并新增一个显式的会话 thinking 更新操作，例如 `updateSessionThinking(sessionId, enabled)`。这个操作要只更新当前 session 的抽象状态，不把 provider 原始字段塞进 store。实现时保持现有 optimistic user message 逻辑不变，避免把 Phase 9 变成 store 大重构。</action>
  <verify>rg -n "updateSessionThinking|thinkingEnabled" desktop/src/renderer/stores/workspace.ts</verify>
  <acceptance_criteria>
    - `desktop/src/renderer/stores/workspace.ts` contains `updateSessionThinking`
    - `desktop/src/renderer/stores/workspace.ts` reads and writes `thinkingEnabled` on `ChatSession`
    - optimistic message flow remains present in `sendMessage`
  </acceptance_criteria>
  <done>renderer store 可以管理会话级 thinking 状态，且不引入 provider 细节。</done>
</task>

<task type="auto">
  <name>Task 2: 为 ChatPage 添加轻量开关、确认交互和 UI 测试</name>
  <files>src/renderer/pages/ChatPage.tsx, tests/phase9-thinking-ui.test.ts</files>
  <read_first>src/renderer/pages/ChatPage.tsx, src/renderer/stores/workspace.ts, .planning/phases/09-chat-thinking-effort/09-RESEARCH.md, desktop/tests/phase15-context-ui.test.ts</read_first>
  <action>在 `ChatPage.tsx` 增加轻量的 thinking 开关或 badge，不引入 low/medium/high/max 选择器。文案应类似 `Thinking: On/Off`，放在不干扰主输入的区域。若当前会话已经存在 assistant 消息，则切换前先弹出确认，明确说明“中途切换会改变后续回复延迟与风格”。保留现有 reasoning `<details>` 折叠展示，不让 reasoning 成为主消息。新增 `tests/phase9-thinking-ui.test.ts`，覆盖开关渲染、已有 assistant 消息时的确认行为、状态 badge 展示。 </action>
  <verify>pnpm --dir desktop exec vitest run tests/phase9-thinking-ui.test.ts</verify>
  <acceptance_criteria>
    - `desktop/src/renderer/pages/ChatPage.tsx` contains `Thinking`
    - ChatPage still contains `<details`-style reasoning rendering path
    - `desktop/tests/phase9-thinking-ui.test.ts` exists
    - test file covers confirmation on mid-conversation toggle
  </acceptance_criteria>
  <done>Chat UI 已具备可控、克制的 reasoning 开关，并有自动化回归测试。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --dir desktop exec vitest run tests/phase9-thinking-ui.test.ts`
- [ ] `rg -n "Thinking" desktop/src/renderer/pages/ChatPage.tsx`
- [ ] ChatPage reasoning details rendering remains present
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- ChatPage 提供轻量开关而不是复杂参数面板
  </success_criteria>

<output>
After completion, create `.planning/phases/09-chat-thinking-effort/09-03-SUMMARY.md`
</output>
