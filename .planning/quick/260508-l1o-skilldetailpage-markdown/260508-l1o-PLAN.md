---
phase: quick-260508-l1o
files_modified:
  - desktop/src/renderer/pages/SkillDetailPage.tsx
must_haves:
  truths:
    - "Skills 详情打开 .md 预览时不再有外层「大卡盒」（无 18px 圆角、无 box-shadow、无 linear-gradient 背景、无 1px border）"
    - "Markdown 内容仍居中 880px 宽（保留可读性约束）"
    - "h1/h2 字号收紧为桌面级，不再出现 36px / 28px clamp 网页 hero 字号"
    - "代码块去渐变 + 圆角 14 → 6，对齐桌面卡片标准"
---

# Plan 260508-l1o: SkillDetailPage Markdown 预览去网页大卡片化

## 5 处具体改动

| # | 元素 | 改动 |
|---|------|------|
| 1 | `.markdown-preview` 外层 | 去 radial-gradient + linear-gradient 双层背景 → 纯 `var(--bg-base)`；padding 24 → 32 48 |
| 2 | `.markdown-preview__surface` 卡盒 | 删 border、border-radius 18px、box-shadow（多层 + 0 20px 48px）、linear-gradient 背景；保留 max-width 880px + margin 0 auto；padding clamp(24,4vw,40) → 0 |
| 3 | `.markdown-preview pre` 代码块 | 去 linear-gradient → rgba(0,0,0,0.3)；去 box-shadow；圆角 14 → 6；padding 16/18 → 14/16 |
| 4 | h1/h2/h3/h4-6 字号 | h1 clamp(28-36)/800 → 24/600（去 border-bottom）；h2 clamp(22-28)/700 → 18/600；h3 18 → 15；h4-6 15 → 13 |
| 5 | blockquote / table 圆角 | blockquote 0/12/12/0 → 0/6/6/0；table 12 → 6 |

不动：
- JSX、`renderSafeSkillMarkdown` 调用、`shouldShowSkillPreviewToggle`、`data-testid="skill-detail-content"`
- 其他视图（image-preview、source code 文本视图）
- 列表 / 链接 / inline code 样式（已经合理）
