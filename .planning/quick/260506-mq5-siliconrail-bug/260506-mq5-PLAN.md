---
phase: quick-260506-mq5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - desktop/src/renderer/pages/ChatPage.tsx
  - desktop/src/renderer/stores/workspace.ts
  - desktop/src/renderer/components/SiliconRail.tsx
autonomous: true
requirements:
  - QUICK-260506-MQ5
---

<objective>
修复硅基员工 SiliconRail 徽章不更新的 bug，并把数字未读计数替换为单点视觉提示（pulse + glow ring）。

Purpose:
- ChatPage 当前在 `session.updated` 分支错误地把 `loadSiliconPersonById` 刷新动作 gating 在「用户当前正在查看该硅基员工」上，导致 @-mention 流（用户在主聊天派发任务，不切到员工视图）永远不会刷新员工摘要徽章。
- 即便修复了 gating，全摘要的 round-trip 仍有延迟；workspace 的 `applySessionUpdate` 应在收到 `updatedSession` 时同步 patch 对应硅基员工的 session summary（`needsApproval` 可由 `chatRunState.phase === "approval"` 推导；`unreadCount/hasUnread` ChatSession 上没有，留给 loadSiliconPersonById 兜底）。
- SiliconRail 的数字未读 badge（`9+` / `!`）不符合本期单点提示设计，改为对 `.status-dot` 加 pulse + glow ring 的单点视觉信号，按 needsApproval > done+hasUnread > running 的优先级表达。

Output:
- 三个文件原子提交，每文件一个 commit；最后统一跑 tsc 校验。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md
@desktop/src/renderer/pages/ChatPage.tsx
@desktop/src/renderer/stores/workspace.ts
@desktop/src/renderer/components/SiliconRail.tsx
@desktop/shared/contracts/silicon-person.ts
@desktop/shared/contracts/session.ts
@desktop/shared/contracts/events.ts

<interfaces>
<!-- 本计划需要直接对照的契约形状，避免 executor 自己再 grep。 -->

From `desktop/shared/contracts/silicon-person.ts`:
```typescript
export type SiliconPersonSessionSummary = {
  id: string;
  title: string;
  status: SiliconPersonStatus;
  unreadCount: number;
  hasUnread: boolean;
  needsApproval: boolean;
  updatedAt: string;
};

export type SiliconPerson = {
  id: string;
  name: string;
  title: string;
  status: SiliconPersonStatus;
  // ...
  sessions: SiliconPersonSessionSummary[];
  unreadCount: number;
  hasUnread: boolean;
  needsApproval: boolean;
  // ...
};
```

From `desktop/shared/contracts/session.ts`:
```typescript
export type ChatSession = {
  id: string;
  title: string;
  siliconPersonId?: string | null;
  // ...
  chatRunState?: {
    runId: string;
    status: ChatRunStatus;          // "running" | "canceling" | "canceled" | "completed" | "failed"
    phase: ChatRunPhase;            // "planning" | "model" | "approval" | "tools" | "persisting"
    activeMessageId?: string;
    lastReason?: string | null;
  } | null;
  messages: ChatMessage[];
};
```

关键事实：
- `ChatSession` 不直接携带 `unreadCount` / `hasUnread` / `needsApproval` 字段。
- `chatRunState.phase === "approval"` 是会话级「待审批」的可推导信号；可以用作 SiliconPersonSessionSummary.needsApproval 的更新源。
- `SiliconPersonStatus`（员工/会话级状态枚举）与 `ChatRunStatus`（运行级状态枚举）是两套，不要混用，本计划不修改 `summary.status`。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ChatPage.tsx — 移除 loadSiliconPersonById 的错误 gating</name>
  <files>desktop/src/renderer/pages/ChatPage.tsx</files>
  <action>
定位 `onSessionStream` 订阅里 `type === "session.updated"` 分支（约 896-909 行）。

当前代码：
```ts
} else if (type === "session.updated" && updatedSession) {
  ws.applySessionUpdate(updatedSession);
  if (
    updatedSession.siliconPersonId
    && updatedSession.siliconPersonId === activeViewSiliconPersonIdRef.current
  ) {
    void ws.loadSiliconPersonById(updatedSession.siliconPersonId).catch((error) => {
      console.error("[chat-page] 刷新硅基员工摘要失败", {
        siliconPersonId: updatedSession.siliconPersonId,
        sessionId: updatedSession.id,
        error: error instanceof Error ? error.message : String(error),
      });
    });
  }
```

改成：
```ts
} else if (type === "session.updated" && updatedSession) {
  ws.applySessionUpdate(updatedSession);
  if (updatedSession.siliconPersonId) {
    void ws.loadSiliconPersonById(updatedSession.siliconPersonId).catch((error) => {
      console.error("[chat-page] 刷新硅基员工摘要失败", {
        siliconPersonId: updatedSession.siliconPersonId,
        sessionId: updatedSession.id,
        error: error instanceof Error ? error.message : String(error),
      });
    });
  }
```

只删掉对 `activeViewSiliconPersonIdRef.current` 的等值判断；其他逻辑（`isActiveViewSession(updatedSession.id)` 那一段、`setCurrentRound(0)` / `setActiveTools(new Map())`）原样保留，不要顺手改其他东西。

理由：@-mention 场景里用户站在主聊天 session（`activeViewSiliconPersonIdRef.current` 为 null），但派发后服务端推送的 `updatedSession.siliconPersonId` 是被 mention 的硅基员工，原 gating 让这种最常见场景永远拿不到刷新摘要，rail 徽章自然也就不更新。

完成后单文件原子提交：
```
git add desktop/src/renderer/pages/ChatPage.tsx
git commit -m "fix(chat-page): refresh silicon-person summary on every session.updated, not only when actively viewing"
```
  </action>
  <verify>
    <automated>git diff HEAD~1 -- desktop/src/renderer/pages/ChatPage.tsx | grep -q "activeViewSiliconPersonIdRef" && exit 1 || exit 0</automated>
  </verify>
  <done>
- `session.updated` 分支不再用 `activeViewSiliconPersonIdRef.current` gating `loadSiliconPersonById`。
- console.error 路径与字段保持不变。
- 该文件已 committed 为单独一个 commit。
  </done>
</task>

<task type="auto">
  <name>Task 2: workspace.ts — applySessionUpdate 同步 patch siliconPersons 摘要</name>
  <files>desktop/src/renderer/stores/workspace.ts</files>
  <action>
定位 `applySessionUpdate` (约 1729-1740 行)。当前实现只更新 `s.sessions[]`：

```ts
applySessionUpdate(updatedSession) {
  set((s) => {
    const sessions = [...s.sessions];
    const index = sessions.findIndex((item) => item.id === updatedSession.id);
    if (index >= 0) {
      sessions[index] = updatedSession;
    } else {
      sessions.unshift(updatedSession);
    }
    return { sessions };
  });
},
```

改为在同一次 `set` 里同时尝试 patch 对应硅基员工的 session summary。规则（严格按下列约束实现，不要扩张）：

1. 维持 `sessions` 的更新逻辑不变（保留原 `sessions = [...s.sessions]; index/findIndex/unshift` 那段）。

2. 新增 siliconPersons patch：
   - 如果 `updatedSession.siliconPersonId` 为 null/undefined → 不动 `siliconPersons`，直接 `return { sessions }`。
   - 否则在 `s.siliconPersons` 中找 `id === updatedSession.siliconPersonId` 的员工：
     - 找不到 → 不动 `siliconPersons`，return `{ sessions }`。
     - 找到 → 在该员工的 `sessions[]` 中找 `id === updatedSession.id` 的 summary：
       - 找不到 → 不动 `siliconPersons`，return `{ sessions }`（loadSiliconPersonById 会兜底）。
       - 找到 → 仅按下表更新该 summary 的字段：

| Summary 字段 | 更新来源 | 说明 |
|---|---|---|
| `needsApproval` | `updatedSession.chatRunState?.phase === "approval"` | ChatRunPhase 枚举里 `"approval"` 即 pending approval |
| `updatedAt` | 留 summary 原值 | ChatSession 上没有 updatedAt 字段，不要改 |
| `unreadCount` / `hasUnread` | 留 summary 原值 | ChatSession 不携带未读信号，由 loadSiliconPersonById 兜底 |
| `status` / `title` / `id` | 留 summary 原值 | SiliconPersonStatus ≠ ChatRunStatus，不要混用 |

3. 如果上一步 summary 的 `needsApproval` 实际发生了变化，重算该员工的 `needsApproval = sessions.some(s => s.needsApproval)`。`hasUnread` 与 `unreadCount` 因为 per-session 字段没动，**不要重算**——保留 `loadSiliconPersonById` 之前的值即可。`status` 不动。

4. 不可变更新：用 `siliconPersons.map(...)` 替换该员工对象，不要原地 mutate；employee 内部的 `sessions` 数组也用 `map(...)` 生成新引用。其他员工对象的引用保持不变（避免无关 React subscriber 重渲）。

5. 全部包在同一个 `set((s) => { ... })` 里，最后 `return { sessions, siliconPersons }`（或当未触发 patch 时只 return `{ sessions }`）。

伪代码骨架（实现时按项目代码风格——双引号、2 空格、保留分号、camelCase）：

```ts
applySessionUpdate(updatedSession) {
  set((s) => {
    const sessions = [...s.sessions];
    const index = sessions.findIndex((item) => item.id === updatedSession.id);
    if (index >= 0) {
      sessions[index] = updatedSession;
    } else {
      sessions.unshift(updatedSession);
    }

    const siliconPersonId = updatedSession.siliconPersonId;
    if (!siliconPersonId) {
      return { sessions };
    }
    const personIndex = s.siliconPersons.findIndex((p) => p.id === siliconPersonId);
    if (personIndex < 0) {
      return { sessions };
    }
    const person = s.siliconPersons[personIndex]!;
    const summaryIndex = person.sessions.findIndex((it) => it.id === updatedSession.id);
    if (summaryIndex < 0) {
      return { sessions };
    }

    const phase = updatedSession.chatRunState?.phase;
    const nextNeedsApproval = phase === "approval";
    const prevSummary = person.sessions[summaryIndex]!;
    if (prevSummary.needsApproval === nextNeedsApproval) {
      return { sessions };
    }
    const nextSummaries = person.sessions.map((item, i) =>
      i === summaryIndex ? { ...item, needsApproval: nextNeedsApproval } : item,
    );
    const nextPerson = {
      ...person,
      sessions: nextSummaries,
      needsApproval: nextSummaries.some((it) => it.needsApproval),
    };
    const siliconPersons = s.siliconPersons.map((p, i) => (i === personIndex ? nextPerson : p));
    return { sessions, siliconPersons };
  });
},
```

不要新增辅助函数、不要复用 `mergeSiliconPersonSessionPayload`（那个 helper 是合并整包 payload，本场景没有 SiliconPerson 实体，仅有 ChatSession）。

完成后单文件原子提交：
```
git add desktop/src/renderer/stores/workspace.ts
git commit -m "fix(workspace): applySessionUpdate patches silicon-person session summary needsApproval inline"
```
  </action>
  <verify>
    <automated>git log -1 --pretty=%s -- desktop/src/renderer/stores/workspace.ts | grep -q "applySessionUpdate"</automated>
  </verify>
  <done>
- `applySessionUpdate` 在同一次 `set` 里完成 sessions[] + siliconPersons[] 的不可变更新。
- 仅 `needsApproval`（per-session）+ 员工级 `needsApproval` 聚合被更新；`unreadCount` / `hasUnread` / `status` 不动。
- 没有引入新 helper。
- 该文件单独 commit。
  </done>
</task>

<task type="auto">
  <name>Task 3: SiliconRail.tsx — 删除数字 badge，状态点改为 pulse + glow ring</name>
  <files>desktop/src/renderer/components/SiliconRail.tsx</files>
  <action>
两块改动：JSX 与 inline `<style>` CSS。**只动 SiliconRailAvatar 与其样式块**，不要动 STATUS_COLOR / STATUS_LABEL / 主组件 SiliconRail 的导出与导航逻辑。

### 3.1 JSX 改动（SiliconRailAvatar 内部）

A. 删除整段 `rail-badge` 渲染（当前 61-68 行）：
```tsx
{person.needsApproval && (
  <span className="rail-badge approval-badge" title="待审批">!</span>
)}
{person.hasUnread && !person.needsApproval && (
  <span className="rail-badge unread-badge" title={`${person.unreadCount} 条未读`}>
    {person.unreadCount > 9 ? "9+" : person.unreadCount}
  </span>
)}
```

B. 计算 attention 类名与 title。在函数体（`statusLabel` 之后、`return` 之前）新增：

```tsx
// 单点视觉提示优先级：needsApproval > done + hasUnread > running > 静态。
let attentionVariant: "yellow" | "green" | null = null;
if (person.needsApproval) {
  attentionVariant = "yellow";
} else if (person.status === "done" && person.hasUnread) {
  attentionVariant = "green";
}

const dotClassName = [
  "status-dot",
  person.status === "running" && !attentionVariant ? "is-running" : null,
  attentionVariant ? `status-dot--attention status-dot--attention-${attentionVariant}` : null,
]
  .filter(Boolean)
  .join(" ");

let titleText = `${person.name} — ${statusLabel}`;
if (person.needsApproval) {
  titleText = `${person.name} — 待审批`;
} else if (person.status === "done" && person.hasUnread) {
  titleText = `${person.name} — 已完成，待查看（${person.unreadCount} 条更新）`;
}
```

C. 把现有 `<span className={...} ... />` 用新变量代入：

```tsx
<span
  className={dotClassName}
  style={{ background: statusColor }}
/>
```

并把按钮 `title` 改成 `title={titleText}`。

注意：`is-running` 与 `status-dot--attention` 是互斥的——上面 attentionVariant 非空时不再加 `is-running`，避免 opacity-fade 与 ring-pulse 同时跑。

### 3.2 CSS 改动（同文件 `<style>{` 块，约 195-218 行）

A. 删除 `.rail-badge` / `.approval-badge` / `.unread-badge` 三条规则（即整块 195-218 行）。

B. 在 `.status-dot.is-running` 与 `silicon-rail-pulse` 关键帧之后新增 attention 规则：

```css
.status-dot--attention {
  animation: silicon-rail-attention 1.4s ease-out infinite;
  transform-origin: center;
}

.status-dot--attention-yellow {
  --rail-attention-color: var(--status-yellow);
}

.status-dot--attention-green {
  --rail-attention-color: var(--status-green);
}

@keyframes silicon-rail-attention {
  0% {
    transform: scale(1);
    box-shadow: 0 0 0 0 color-mix(in srgb, var(--rail-attention-color, var(--status-yellow)) 60%, transparent);
  }
  70% {
    transform: scale(1.08);
    box-shadow: 0 0 0 6px color-mix(in srgb, var(--rail-attention-color, var(--status-yellow)) 0%, transparent);
  }
  100% {
    transform: scale(1);
    box-shadow: 0 0 0 0 color-mix(in srgb, var(--rail-attention-color, var(--status-yellow)) 0%, transparent);
  }
}
```

设计说明：
- 用 CSS 变量 `--rail-attention-color` 解耦颜色，按变体类切换；不要为黄/绿各写一份独立 keyframes。
- `box-shadow: 0 0 0 0 → 0 0 0 6px` 形成 ring pulse；同时 scale 1 → 1.08 加强存在感。
- `color-mix(in srgb, ... , transparent)` 现代浏览器支持（Electron 33 / Chromium ≥ 119 已具备）；项目其他 CSS 已有 `var(--status-yellow)` 等 token。
- 不要触碰已有 `.status-dot.is-running` 与 `silicon-rail-pulse` 规则——`running` 状态视觉保持原样。

完成后单文件原子提交：
```
git add desktop/src/renderer/components/SiliconRail.tsx
git commit -m "feat(silicon-rail): replace numeric unread badge with pulse + glow-ring single-point cue"
```
  </action>
  <verify>
    <automated>git grep -n "rail-badge\|approval-badge\|unread-badge" -- desktop/src/renderer/components/SiliconRail.tsx | wc -l | grep -qx "0"</automated>
  </verify>
  <done>
- JSX 中不再出现 `rail-badge` / `approval-badge` / `unread-badge` 三个 className。
- `.status-dot--attention` + 颜色变体类 + `silicon-rail-attention` keyframes 均已加入。
- needsApproval 优先级 > done + hasUnread > running > 静态，逻辑由 `attentionVariant` 单变量驱动。
- title 文案按规范切换。
- 该文件单独 commit。
  </done>
</task>

</tasks>

<verification>
全部三个 commit 落盘后，统一跑一次 desktop renderer 的 typecheck（用户明确要求一次性写完后再验证，不要每个 task 都跑）：

```bash
pnpm --dir desktop tsc --noEmit -p tsconfig.renderer.json
```

如果上面命令在 `desktop/package.json` 里没有直接对应 script，按 `desktop/package.json` 的 typecheck 脚本走（例如 `pnpm --dir desktop typecheck`）；执行前先 `cat desktop/package.json | grep -A 5 scripts` 确认。

验收（人工）：
1. 用户在主聊天 @ 一个硅基员工并派发 → SiliconRail 该员工头像 status-dot 出现 pulse + ring（无数字 badge 出现在 rail 上的任何地方）。
2. 用户点击该头像进入硅基员工聊天 → 既有 `markSiliconPersonSessionRead` 清掉 `hasUnread` → pulse 消失。
3. 当 `needsApproval=true` 时，无论 `hasUnread` 是否为 true，rail 上显示黄色 ring（needsApproval 视觉优先级最高）。
</verification>

<success_criteria>
- 三个文件各自一个原子 commit，commit 顺序：ChatPage → workspace → SiliconRail（不强制，但每个 commit 自包含、可独立 revert）。
- `pnpm --dir desktop tsc ...` 对 renderer 子项目 typecheck 通过。
- `git grep "rail-badge"` 在 `desktop/src/renderer/components/SiliconRail.tsx` 内零命中。
- `git grep "activeViewSiliconPersonIdRef" desktop/src/renderer/pages/ChatPage.tsx` 中 `session.updated` 分支内零命中（其他分支如 `runtime.status` / `context.limit_warning` 中保留的引用不要动）。
- `applySessionUpdate` 仍然是 store 暴露的同名 action，外部 caller 签名 `(updatedSession: ChatSession) => void` 不变。
</success_criteria>

<output>
After completion, append a short note to `.planning/STATE.md` Quick Tasks Completed table:
- `260506-mq5` — 修复 SiliconRail 徽章不更新（@-mention 不刷新摘要）+ 数字 badge 改单点 pulse + glow ring。

Per project workflow, also drop a brief SUMMARY at `.planning/quick/260506-mq5-siliconrail-bug/260506-mq5-SUMMARY.md` recording the three commits and the typecheck outcome.
</output>
