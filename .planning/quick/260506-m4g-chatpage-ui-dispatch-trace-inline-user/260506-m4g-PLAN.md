---
quick_id: 260506-m4g
type: execute
wave: 1
depends_on: []
files_modified:
  - desktop/src/renderer/pages/ChatPage.tsx
autonomous: true
requirements: [UI-A-trace-inline, UI-A-trace-autoclear, UI-A-trace-lightweight, UI-B-user-right]

must_haves:
  truths:
    - "投递痕迹卡片不再常驻底部 — 出现在消息流末尾，会被新消息推上去并随时间线滚动"
    - "投递痕迹 5 秒后自动消失，不需要用户手动关闭"
    - "投递痕迹视觉轻量 — 不再是厚卡片，更像一行系统提示"
    - "用户消息（role-user）头像和气泡都靠右；assistant 布局保持不变"
    - "组件 unmount 时挂起的 setTimeout 被清理，不触发 React state-on-unmounted 警告"
  artifacts:
    - path: "desktop/src/renderer/pages/ChatPage.tsx"
      provides: "ChatPage 唯一改动文件，覆盖 dispatchTraces JSX/CSS + 用户消息靠右 CSS"
  key_links:
    - from: "setDispatchTraces push (~L1084)"
      to: "setTimeout cleanup ref"
      via: "useRef<Set<ReturnType<typeof setTimeout>>>"
      pattern: "setTimeout.*5000"
    - from: ".dispatch-traces JSX"
      to: "timeline section"
      via: "JSX 重定位到 <section className=\"timeline-panel\">/<div className=\"timeline\"> 内、messages.map 闭合之后"
      pattern: "dispatch-traces"
    - from: ".role-user .message-row"
      to: "flex-direction: row-reverse"
      via: "新增/覆盖 .role-user 派生 CSS 规则"
      pattern: "role-user"
---

<objective>
ChatPage UI 两处微调，单文件原子改动。

1. 投递痕迹（dispatchTraces）：
   - 不再常驻输入框上方；改为渲染在消息流末尾，会随消息流滚动。
   - 推入后 5 秒自动消失（清理 timer 防 unmount 警告）。
   - 视觉轻量化：去掉卡片背景/边框，变成一行系统提示式的浅色文字。
2. 用户消息靠右：
   - `.role-user` 整行 row-reverse，头像在右、气泡在右。
   - 用户气泡加上 `var(--accent-soft, rgba(64, 180, 220, 0.08))` 浅底 + `var(--radius-xl)` 圆角 + `var(--glass-border)` 边框。
   - assistant 布局零侵入。

Purpose: 让 ChatPage 视觉更接近主流 IM（用户右、AI 左）+ 减少底部噪声（投递条不再永久挤占视觉份量）。
Output: desktop/src/renderer/pages/ChatPage.tsx 一个文件 diff。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@desktop/docs/ui-style-guide.md
@desktop/src/renderer/pages/ChatPage.tsx

# 关键参考定位（行号基于读取时快照，执行时以当前文件实际定位为准）

# A 改动相关锚点：
- L337         dispatchTraces useState 定义（不动数据形态）
- L1080-1093   sendMessageToRuntime 内 setDispatchTraces push 入口（要在 push 后挂 5s timer）
- L1521-1528   <section className="timeline-panel"> 起点 + <div className="timeline"> 起点
- L1909-1910   </div>(timeline) </section>(timeline-panel) 结束 — A1 重定位的目标插入点：
                 在 L1909 </div>(timeline) 之前、messages.map 闭合（L1908 })}）之后
- L1923-1926   PlanStatePanel 渲染（保留位置不动）
- L1928-1945   原 dispatch-traces JSX（要删除原位、移入 timeline 内）
- L2422-2427   .dispatch-traces / .dispatch-trace-card / .dispatch-trace-dot / .dispatch-trace-text /
               .dispatch-trace-link CSS（要轻量化）

# B 改动相关锚点：
- L1605        <div className={`message-row role-${message.role}`}>
- L1622-1720   .message-body 内部结构（包含 message-header / message-content / message-details /
               token-usage-badge / form-submission-summary / renderUiFields）
- L2230-2237   现有 .message-row / .message-avatar / .role-assistant / .role-user 规则段落 —
               B1 新规则就插在这一段尾部（L2237 之后），覆盖原来 row + flex-start 的简单实现
- L2240        .message-body 已有 `display: flex; flex-direction: column; gap: 8px;` —
               不需要再补 display: flex（B1 里 .role-user .message-body 只需 align-items: flex-end + max-width）
- L2241        .message-header 已有 `display: flex; align-items: baseline; gap: 8px;` —
               不需要再补 display: flex（B1 里 .role-user .message-header 只需 justify-content: flex-end）

<interfaces>
<!-- dispatchTraces state shape（不改）：-->
useState<Array<{ id: string; personName: string; personId: string; content: string; timestamp: string }>>

<!-- 现有 message-body / message-header CSS（确认已经是 flex，不要重复补 display: flex）：-->
.message-body  { display: flex; flex-direction: column; gap: 8px; flex: 1; min-width: 0; }
.message-header { display: flex; align-items: baseline; gap: 8px; font-size: 13px; ... }

<!-- 现有 .role-user 规则（要被 Task B 的新规则覆盖/扩展，不要保留旧的 flex-direction: row）：-->
.role-user { flex-direction: row; }                          /* ← 改为 row-reverse */
.role-user .message-body { align-items: flex-start; }        /* ← 改为 flex-end + max-width 72% */
.role-user .message-header { margin-right: 0; }              /* ← 加 justify-content: flex-end */
.role-user .message-content { background: transparent; padding: 0; border: none; max-width: 100%; }
                                                              /* ← 改为 accent-soft 底 + radius-xl + border + 收紧 max-width */
.role-user .message-avatar { background: transparent; border-color: var(--glass-border); color: var(--text-primary); }
                                                              /* ← 保持不动 */
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (UI-A): 投递痕迹卡片移入消息流 + 5s 自动消失 + 视觉轻量化</name>
  <files>desktop/src/renderer/pages/ChatPage.tsx</files>
  <action>
单文件三处改动，全部针对 dispatchTraces：

(A1) JSX 重定位
- 删除原 L1928-1945 的 `{dispatchTraces.length > 0 && ( <div className="dispatch-traces">...</div> )}` 整块（位于 PlanStatePanel 之后、background-task-panel 之前）。
- 把同一块 JSX 移入 `<section className="timeline-panel">` 内：插入到 messages.map 闭合之后（L1908 `})}` 之后）、`</div>` (timeline, L1909) 之前。结果：dispatchTraces 跟随 timeline 内容滚动；新消息推入会把 trace 顶上去。

(A2) 5 秒自动清理 + unmount 安全
- 在组件顶层（与其它 useRef 同段，例如 L320 timelineStickToBottomRef 附近）新增一个 ref 集中管理 timer：
    const dispatchTraceTimersRef = useRef<Set<ReturnType<typeof setTimeout>>>(new Set());
- 新增一个 useEffect 仅做 unmount 清理（依赖数组为 []）：
    useEffect(() => {
      return () => {
        dispatchTraceTimersRef.current.forEach((t) => clearTimeout(t));
        dispatchTraceTimersRef.current.clear();
      };
    }, []);
- 在 sendMessageToRuntime 内 setDispatchTraces push 处（L1084-1093，先生成 traceId 再用同一个 id push 并启动 timer，避免再读 prev 末尾元素）：
    const newTraceId = `${Date.now()}-${mentionTargetSiliconPersonId}`;
    setDispatchTraces((prev) => [
      ...prev,
      {
        id: newTraceId,
        personName: person.name,
        personId: person.id,
        content: draft,
        timestamp: new Date().toLocaleTimeString(),
      },
    ]);
    const timer = setTimeout(() => {
      setDispatchTraces((prev) => prev.filter((t) => t.id !== newTraceId));
      dispatchTraceTimersRef.current.delete(timer);
    }, 5000);
    dispatchTraceTimersRef.current.add(timer);
- 数据形态零变更（id/personName/personId/content/timestamp 五字段保持）。

(A3) CSS 轻量化（替换 L2422-2427 现有规则）
- `.dispatch-traces` 容器：保留 max-width: 1200px; margin: 0 auto; 但把 padding 收到 `4px 48px`，gap 收到 4px。
- `.dispatch-trace-card` 改为：
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 4px 8px;
    background: transparent;
    border: none;
    border-radius: 0;
    font-size: 12px;
    line-height: 1.4;
    color: var(--text-muted);
    opacity: 0.85;
- `.dispatch-trace-dot` 缩到 5px、保留 var(--accent-cyan)。
- `.dispatch-trace-text` 颜色改为 var(--text-muted)；保留 ellipsis。
- `.dispatch-trace-link` 字号降到 11px，颜色保持 var(--accent-cyan)，hover 仍 underline。
- 不引入硬编码 8/10/12/14px 圆角；直接 0/无圆角即可（这是“一行系统提示”不是卡片，圆角不再适用）。
- 不动 token；保持 var(--accent-cyan) / var(--text-muted) / var(--glass-border) 等约束。

约束：
- 不修改 dispatchTraces 数据形态、不重命名 state、不抽组件。
- 文件本地风格：双引号、分号、2 空格缩进。
- 风格指南：`desktop/docs/ui-style-guide.md`（不要硬编码非 token 圆角）。
  </action>
  <verify>
读 diff 自检：
- 原 L1928-1945 dispatch-traces JSX 块已不在 PlanStatePanel 之后那个位置出现。
- 同样的 JSX 出现在 timeline-panel section 内、`</div>` (timeline) 之前。
- sendMessageToRuntime 里出现 newTraceId 常量、setTimeout(..., 5000)、dispatchTraceTimersRef.current.add(timer)。
- 顶层 useEffect 含 dispatchTraceTimersRef.current.forEach(clearTimeout)。
- L2422-2427 段 .dispatch-trace-card 的 background/border/border-radius 三项分别为 transparent/none/0。
- typecheck 留到 Task 3 一起跑。
  </verify>
  <done>
JSX 已迁入消息流；setTimeout(5s) 推入后挂载，unmount useEffect 已清理；dispatch-trace-card CSS 不再有 background/border 卡片视觉；dispatchTraces 字段形态、push 触发条件均未变。
  </done>
</task>

<task type="auto">
  <name>Task 2 (UI-B): 用户消息（role-user）头像 + 气泡靠右</name>
  <files>desktop/src/renderer/pages/ChatPage.tsx</files>
  <action>
仅改 CSS。定位到 L2230-2237 段（.message-row / .message-avatar / .role-assistant / .role-user 现有规则），替换 L2233-2236 这四行旧的 .role-user 规则：

旧（删除）：
    .role-user { flex-direction: row; }
    .role-user .message-body { align-items: flex-start; }
    .role-user .message-header { margin-right: 0; }
    .role-user .message-content { background: transparent; padding: 0; border: none; max-width: 100%; }

新（写入）：
    .role-user { flex-direction: row-reverse; }
    .role-user .message-body { align-items: flex-end; max-width: 72%; }
    .role-user .message-header { justify-content: flex-end; margin-right: 0; }
    .role-user .message-content {
      background: var(--accent-soft, rgba(64, 180, 220, 0.08));
      padding: 10px 14px;
      border-radius: var(--radius-xl);
      border: 1px solid var(--glass-border);
    }

保持不动：
- L2237 `.role-user .message-avatar { background: transparent; border-color: var(--glass-border); color: var(--text-primary); }` 维持原样（头像视觉一致，只是位置随 row-reverse 漂到右侧）。
- L2231 `.message-avatar` 通用规则不动。
- L2232 `.role-assistant .message-avatar` 不动；assistant 整体布局保持不变。
- L2240 `.message-body` 通用规则（已有 display: flex; flex-direction: column; gap: 8px;）不动 — 不要在 .role-user .message-body 里重复 display: flex。
- L2241 `.message-header` 通用规则（已有 display: flex; align-items: baseline; gap: 8px;）不动 — 不要在 .role-user .message-header 里重复 display: flex。

副作用确认（只读 spot-check，不改）：
- 用户消息一般无 reasoning / tool_calls / token-usage-badge —— 这些只在 assistant 分支渲染（见 L1632, L1672, L1694），不会被 role-user 新规则影响。
- form-submission-summary（L1700）和 renderUiFields（L1719）位于 message-body 内 —— max-width: 72% + align-items: flex-end 会让它们靠右且最大宽度受限；这是预期行为，不需要额外覆盖。
- .message-content 的 padding 影响 dangerouslySetInnerHTML 渲染的 markdown 块（L1665-1670）；10px 14px 内边距对于段落/列表/code/pre 都正常，不会破坏现有 .message-content 子选择器（L2247-2256）。

约束：
- 不抽组件、不改 message-row JSX。
- assistant 布局零侵入（只动 .role-user 派生规则，不动 .message-row / .role-assistant 通用规则）。
- 圆角必须用 token：var(--radius-xl)。
- 颜色必须用 token：var(--accent-soft, ...) / var(--glass-border)（带 fallback 是因为 global.css 不一定声明了 --accent-soft；fallback 为 rgba(64, 180, 220, 0.08)，与 task spec 一致）。
- 文件本地风格：双引号、分号、2 空格缩进。
  </action>
  <verify>
读 diff 自检：
- L2233 `.role-user` 的 flex-direction 是 row-reverse。
- `.role-user .message-body` 含 `align-items: flex-end` 和 `max-width: 72%`。
- `.role-user .message-header` 含 `justify-content: flex-end`。
- `.role-user .message-content` 含 `var(--accent-soft, rgba(64, 180, 220, 0.08))`、`var(--radius-xl)`、`var(--glass-border)`。
- assistant 相关规则（L2232, L2229 .timeline 等）未被改动。
- typecheck 留到 Task 3 一起跑。
  </verify>
  <done>
用户消息整行 row-reverse；用户气泡靠右、有浅底 + var(--radius-xl) 圆角 + glass-border 边框；assistant 布局未变；CSS 仅使用 token（无硬编码 8/10/12/14 px 圆角）。
  </done>
</task>

<task type="auto">
  <name>Task 3 (Verify): typecheck 单一验证</name>
  <files>desktop/src/renderer/pages/ChatPage.tsx</files>
  <action>
Task 1 + Task 2 实现完成后，跑 desktop 端 TS 类型检查，作为该 quick 任务的唯一自动化验证（per memory: 多 Task 实现时不要中途测试）。

执行（在 desktop/ 目录下）：
    pnpm tsc --noEmit -p tsconfig.renderer.json

如果 tsconfig.renderer.json 在仓库内不可用作此命令的入口（检查 desktop/package.json 的 scripts），fallback：
    pnpm typecheck   # 若 package.json 暴露此 script
或：
    pnpm tsc --noEmit

预期：0 errors。任何与 ChatPage.tsx 相关的新错误必须修掉再判定 done。
  </action>
  <verify>
    <automated>cd desktop && pnpm tsc --noEmit -p tsconfig.renderer.json</automated>
  </verify>
  <done>
typecheck 0 errors；ChatPage.tsx 新增/修改的代码（useRef/useEffect/setTimeout/setDispatchTraces 调用、CSS 字符串）均通过 TS 编译。
  </done>
</task>

</tasks>

<verification>
全部 3 个 task 完成后：
- `cd desktop && pnpm tsc --noEmit -p tsconfig.renderer.json` 返回 0 errors。
- 手工读 diff 确认：单一文件 desktop/src/renderer/pages/ChatPage.tsx；改动范围限定在 (a) L320 附近 useRef + 一段新 useEffect、(b) L1080 附近 setDispatchTraces push + setTimeout、(c) L1521-1528..L1910 timeline section 内插入 dispatch-traces JSX、(d) L1928-1945 旧 dispatch-traces JSX 删除、(e) L2230-2237 段 .role-user 派生规则替换、(f) L2422-2427 段 .dispatch-trace-* CSS 轻量化。
- 没有触碰其他文件、没有重命名、没有抽组件。
</verification>

<success_criteria>
- 投递痕迹现在显示在消息流末尾，并随时间线滚动；新消息会把它推上去。
- 投递痕迹推入 5 秒后自动消失；组件 unmount 时挂起的 timer 被 clearTimeout 清理。
- 投递痕迹视觉轻量：透明背景、无边框、12px 字、轻度 opacity，不再像卡片。
- 用户消息整行 row-reverse：头像在右、气泡在右、header 文字也靠右。
- 用户气泡有 `var(--accent-soft, rgba(64, 180, 220, 0.08))` 浅底 + `var(--radius-xl)` 圆角 + `var(--glass-border)` 边框。
- assistant 消息渲染（reasoning details / tool_calls details / token-usage-badge / 全宽 markdown）零变化。
- typecheck 0 errors。
</success_criteria>

<output>
After completion, create `.planning/quick/260506-m4g-chatpage-ui-dispatch-trace-inline-user/260506-m4g-SUMMARY.md` summarizing:
- 3 tasks 全部完成
- 单文件改动列表（用 file:line 锚定）
- typecheck 通过
- 任何执行期发现的偏离（例如 L 行号在执行时漂移）
</output>
