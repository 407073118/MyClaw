---
phase: quick-260508-hh3-linear
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - desktop/src/renderer/pages/LoginPage.tsx
autonomous: true
requirements:
  - LOGIN-LINEAR-01
must_haves:
  truths:
    - "登录页打开后，用户立刻看到一个无边框单列内容（顶部带框 logo + 三层文字 + 表单 + 主 CTA），背景中央偏上有一团 cyan 径向光晕"
    - "用户能在账号、密码两个输入框里输入文字，密码字段右侧的眼睛图标可切换显示/隐藏明文"
    - "点击登录按钮后，按钮进入 loading 状态（spinner + 文案变 '正在登录...'），auth.login 仍按原签名调用"
    - "登录失败时，表单上方显示带 AlertCircle 图标的错误条，整列内容横向抖动一次（约 200ms）"
    - "页面左下角显示 'V0.1.0 · DEV/PRE/PROD' 这种小字版本与环境标签，颜色为 muted"
    - "页面 main 区背景色为 #08090A（比 --bg-base 略深），TitleBar 不被改动"
    - "现有 data-testid（desktop-login-view / desktop-login-account / desktop-login-password / desktop-login-submit / desktop-login-error）全部保留，定位仍然可用"
  artifacts:
    - path: "desktop/src/renderer/pages/LoginPage.tsx"
      provides: "Linear 风极简单列登录页（重写 JSX + inline <style>）"
      contains: "logo-frame, eyebrow MYCLAW DESKTOP, login-form, submit-button, error-banner, env-footer"
  key_links:
    - from: "desktop/src/renderer/pages/LoginPage.tsx"
      to: "desktop/src/renderer/stores/auth.ts -> auth.login({ account, password })"
      via: "form onSubmit 调用，签名/响应处理保持不变"
      pattern: "auth\\.login\\(\\{\\s*account,\\s*password\\s*\\}\\)"
    - from: "desktop/src/renderer/pages/LoginPage.tsx"
      to: "desktop/config -> APP_ENV_NAME"
      via: "import { APP_ENV_NAME } from \"../../../config\"; 渲染左下角环境标签"
      pattern: "APP_ENV_NAME"
    - from: "desktop/src/renderer/pages/LoginPage.tsx"
      to: "lucide-react -> Eye, EyeOff, ArrowRight, AlertCircle"
      via: "import { Eye, EyeOff, ArrowRight, AlertCircle } from \"lucide-react\";"
      pattern: "from \"lucide-react\""
---

<objective>
桌面端登录页 Linear-style 极简单列重做。把 LoginPage 当前的"卡片 + 显示密码 checkbox"风格替换成 Linear / 1Password 8 风格的极简单列：无边框居中列、品牌方框 logo、单 cyan 光晕背景、label-on-top 表单、眼睛图标切换密码可见性、错误条带抖动、左下角版本+环境角标。

Purpose: 让首屏从"网页登录卡"升级到"原生桌面应用入口"观感，与 desktop UI Style Guide（Linear-anchored）一致；登录是用户每天看到的第一个屏幕，需要一次性建立专业感。
Output: 重写后的 desktop/src/renderer/pages/LoginPage.tsx（仅此一个文件）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@desktop/src/renderer/pages/LoginPage.tsx
@desktop/src/renderer/components/TitleBar.tsx
@desktop/src/renderer/styles/global.css
@desktop/docs/ui-style-guide.md
@desktop/config/index.ts
@desktop/src/renderer/stores/auth.ts
@desktop/package.json

<interfaces>
<!-- 关键契约抽取，executor 不需要再去 grep。 -->

From desktop/src/renderer/stores/auth.ts (签名不能动):
```typescript
type AuthLoginRequest = { account: string; password: string };
type AuthLoginResponse = {
  accessToken: string;
  refreshToken: string;
  expiresIn: number;
  user: AuthUser;
};
// 在组件里照旧用：
//   const auth = useAuthStore();
//   await auth.login({ account, password });
```

From desktop/config/index.ts:
```typescript
// 用于左下角环境角标：
export const APP_ENV_NAME: "development" | "pre" | "production";
// 渲染时映射 development -> "DEV", pre -> "PRE", production -> "PROD"
```

From desktop/package.json -> dependencies:
- lucide-react ^0.468.0  (已安装，可以直接 import Eye/EyeOff/ArrowRight/AlertCircle)
- 不要新增依赖

Existing data-testid（必须保留，原 LoginPage 已经在用）：
- desktop-login-view    -> 挂在 <main> 上
- desktop-login-account -> 账号 input
- desktop-login-password-> 密码 input
- desktop-login-submit  -> 提交按钮
- desktop-login-error   -> 错误信息容器
（无现有单测引用其它 selector，也没有针对 "显示密码 checkbox" 的断言。grep `desktop-login` / `LoginPage` 在 desktop/tests/ 下零命中。）

CSS tokens 可直接引用（来自 desktop/src/renderer/styles/global.css :root）:
- --accent-cyan: #10a37f
- --text-primary / --text-secondary / --text-muted
- --radius-md: 6px (按钮、输入框)
- --bg-base: #0c0c0c (作为对比，main 用更深的 #08090A 而不是它)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重写 LoginPage.tsx 为 Linear 风极简单列</name>
  <files>desktop/src/renderer/pages/LoginPage.tsx</files>
  <action>
完整重写 desktop/src/renderer/pages/LoginPage.tsx 一个文件。保留以下逻辑骨架不动：
- 顶部 imports：useState / useMemo / useNavigate / useLocation / useShellStore / useAuthStore / TitleBar 全部保留
- loginErrorMessageMap 字典原样保留
- redirectTarget useMemo 解析逻辑原样保留
- handleLogin async 函数：try/catch、auth.login 调用签名、error.message 解析 code、navigate(redirectTarget, { replace: true })、所有 console.info/console.warn 业务日志全部保留并仍然带 [desktop-login] 前缀

新增/替换：

1) 新增 import:
   - 从 "lucide-react" 引入 Eye, EyeOff, ArrowRight, AlertCircle
   - 从 "../../../config" 引入 APP_ENV_NAME（注意：当前 LoginPage 在 src/renderer/pages/，到 desktop/config 的相对路径是 ../../../config）。如有路径报错，按 desktop/tsconfig.json 的别名做 fallback：先尝试相对路径，不通就用 import { APP_ENV_NAME } from "../../../../config"。

2) 新增常量：
   - const APP_VERSION = "v0.1.0"; // 与 desktop/package.json 当前版本一致，硬编码即可
   - const ENV_LABEL_MAP: Record<string, string> = { development: "DEV", pre: "PRE", production: "PROD" };

3) 新增 state:
   - const [shakeNonce, setShakeNonce] = useState(0); // 错误抖动触发计数

4) 在 catch 分支里：现有 setErrorMessage 调用之后追加 setShakeNonce((n) => n + 1)，保证每次失败都重新触发 shake 动画（用 key 或 className + 计数器都行；建议给 .login-column 加 className `shake-${shakeNonce}` 或者用内联 key={shakeNonce} 让 React 在每次出错时重新 mount 抖动元素 —— 实现上更稳的方式：把 className 写成 `login-column${errorMessage ? " is-shaking" : ""}`，并用 useEffect 在 shakeNonce 变化时给元素 remove/add `is-shaking`；最简实现：直接用 key={shakeNonce} 包一层 wrapper，CSS 里 .login-column 带 animation: shake 200ms ease，shakeNonce 变化时整列重挂一次触发动画）。任选一种简洁可读的做法即可，不要让动画卡在第一次出错。

5) JSX 结构（替换整个 return 块）：

```tsx
return (
  <div className="app-root-wrapper">
    <TitleBar />
    <main data-testid="desktop-login-view" className="login-page">
      {/* cyan 光晕 blob */}
      <div className="login-glow" aria-hidden="true" />

      {/* 居中单列；错误时整列抖动 */}
      <section className="login-column" key={shakeNonce}>
        {/* 1. 品牌 logo（带框） */}
        <div className="login-brand">
          <div className="login-brand__frame">
            <svg viewBox="0 0 24 24" width="32" height="32" aria-hidden="true">
              <path
                fill="currentColor"
                d="M12.06 2.75 6.18 20h2.36l1.32-4.01h4.27L15.47 20h2.35L12.06 2.75Zm0 5.41 1.37 4.18h-2.76l1.39-4.18Z"
              />
              <path fill="currentColor" opacity="0.34" d="m12.08 9.84 2.05 6.15H9.86z" />
            </svg>
          </div>
        </div>

        {/* 2. 三层文字 */}
        <div className="login-copy">
          <span className="login-eyebrow">MYCLAW DESKTOP</span>
          <h1 className="login-title">登录你的企业工作空间</h1>
          <p className="login-subtitle">使用与 Cloud 一致的企业账号密码</p>
        </div>

        {/* 3. 错误 banner（在表单上方） */}
        {errorMessage && (
          <div data-testid="desktop-login-error" className="error-banner" role="alert">
            <AlertCircle size={14} aria-hidden="true" />
            <span>{errorMessage}</span>
          </div>
        )}

        {/* 4. 表单 */}
        <form className="login-form" onSubmit={handleLogin}>
          <label className="field">
            <span className="field__label">企业账号</span>
            <input
              value={account}
              onChange={(e) => setAccount(e.target.value.trim())}
              data-testid="desktop-login-account"
              type="text"
              autoComplete="username"
              placeholder="请输入企业账号"
              required
            />
          </label>

          <label className="field">
            <span className="field__label">登录密码</span>
            <div className="field__password-wrap">
              <input
                value={password}
                onChange={(e) => setPassword(e.target.value)}
                data-testid="desktop-login-password"
                type={showPassword ? "text" : "password"}
                autoComplete="current-password"
                placeholder="请输入登录密码"
                required
              />
              <button
                type="button"
                className="field__eye"
                onClick={() => setShowPassword((v) => !v)}
                aria-label={showPassword ? "隐藏密码" : "显示密码"}
                tabIndex={-1}
              >
                {showPassword ? <EyeOff size={16} /> : <Eye size={16} />}
              </button>
            </div>
          </label>

          <button
            data-testid="desktop-login-submit"
            type="submit"
            className="submit-button"
            disabled={pending}
          >
            {pending ? (
              <>
                <span className="submit-button__spinner" aria-hidden="true" />
                <span>正在登录...</span>
              </>
            ) : (
              <>
                <span>登录</span>
                <ArrowRight size={16} aria-hidden="true" />
              </>
            )}
          </button>
        </form>
      </section>

      {/* 5. 左下角版本/环境角标 */}
      <div className="login-footer" aria-hidden="true">
        <span>{APP_VERSION}</span>
        <span className="login-footer__sep"> · </span>
        <span>{ENV_LABEL_MAP[APP_ENV_NAME] ?? APP_ENV_NAME.toUpperCase()}</span>
      </div>

      <style>{`
        .app-root-wrapper {
          display: flex;
          flex-direction: column;
          height: 100vh;
          overflow: hidden;
        }

        .login-page {
          position: relative;
          flex: 1;
          display: grid;
          place-items: center;
          padding: 32px;
          overflow: hidden;
          background: #08090A;
        }

        /* cyan 径向光晕 */
        .login-glow {
          position: absolute;
          top: 8%;
          left: 50%;
          transform: translateX(-50%);
          width: 600px;
          height: 600px;
          border-radius: 50%;
          background: radial-gradient(circle, rgba(16, 163, 127, 0.18) 0%, rgba(16, 163, 127, 0) 60%);
          filter: blur(40px);
          pointer-events: none;
          z-index: 0;
        }

        /* 居中单列 */
        .login-column {
          position: relative;
          z-index: 1;
          width: min(400px, 100%);
          display: flex;
          flex-direction: column;
          gap: 32px;
          align-items: stretch;
          animation: login-column-in 400ms ease-out both, login-shake 200ms ease;
        }

        /* 首次入场：仅初始一次会触发 in，shake 在 key 变化重挂时触发 */
        @keyframes login-column-in {
          0% { opacity: 0; transform: translateY(8px); }
          100% { opacity: 1; transform: translateY(0); }
        }

        @keyframes login-shake {
          0%, 100% { transform: translateX(0); }
          25% { transform: translateX(-4px); }
          75% { transform: translateX(4px); }
        }

        /* 品牌 logo 带框 */
        .login-brand {
          display: flex;
          flex-direction: column;
          align-items: center;
          gap: 24px;
        }

        .login-brand__frame {
          width: 56px;
          height: 56px;
          padding: 12px;
          display: grid;
          place-items: center;
          border-radius: 14px;
          background: rgba(16, 163, 127, 0.08);
          border: 1px solid rgba(16, 163, 127, 0.2);
          color: var(--accent-cyan);
          box-sizing: border-box;
        }

        /* 三层文字 */
        .login-copy {
          display: flex;
          flex-direction: column;
          gap: 8px;
          align-items: center;
          text-align: center;
        }

        .login-eyebrow {
          font-size: 11px;
          font-weight: 700;
          text-transform: uppercase;
          letter-spacing: 0.12em;
          color: var(--text-muted);
        }

        .login-title {
          margin: 0;
          font-size: 24px;
          font-weight: 600;
          letter-spacing: -0.01em;
          color: var(--text-primary);
          line-height: 1.25;
        }

        .login-subtitle {
          margin: 0;
          font-size: 13px;
          font-weight: 400;
          line-height: 1.6;
          color: var(--text-secondary);
        }

        /* 错误 banner */
        .error-banner {
          display: flex;
          align-items: flex-start;
          gap: 8px;
          padding: 10px 12px;
          border-radius: var(--radius-md);
          background: rgba(239, 68, 68, 0.10);
          border: 1px solid rgba(239, 68, 68, 0.20);
          color: #fca5a5;
          font-size: 13px;
          line-height: 1.5;
        }

        .error-banner svg {
          margin-top: 2px;
          flex-shrink: 0;
        }

        /* 表单 */
        .login-form {
          display: flex;
          flex-direction: column;
          gap: 16px;
        }

        .field {
          display: flex;
          flex-direction: column;
          gap: 8px;
        }

        .field__label {
          font-size: 13px;
          font-weight: 500;
          color: var(--text-secondary);
        }

        .field input {
          height: 42px;
          background: rgba(255, 255, 255, 0.03);
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: var(--radius-md);
          padding: 0 14px;
          font-size: 14px;
          color: var(--text-primary);
          transition: border-color 0.15s ease, background 0.15s ease;
          width: 100%;
          box-sizing: border-box;
        }

        .field input::placeholder {
          color: var(--text-muted);
        }

        .field input:hover:not(:focus) {
          border-color: rgba(255, 255, 255, 0.15);
        }

        .field input:focus {
          outline: none;
          border-color: var(--accent-cyan);
        }

        /* 密码字段 wrapper（眼睛图标 absolute） */
        .field__password-wrap {
          position: relative;
        }

        .field__password-wrap input {
          padding-right: 44px;
        }

        .field__eye {
          position: absolute;
          right: 8px;
          top: 50%;
          transform: translateY(-50%);
          width: 32px;
          height: 32px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          background: transparent;
          border: none;
          color: var(--text-muted);
          cursor: pointer;
          transition: color 0.15s ease;
        }

        .field__eye:hover {
          color: var(--text-primary);
        }

        .field__eye:focus-visible {
          outline: 2px solid var(--accent-cyan);
          outline-offset: -2px;
          border-radius: var(--radius-md);
        }

        /* 主 CTA —— 登录是 chrome-level entry，例外允许实心填充 */
        .submit-button {
          height: 44px;
          width: 100%;
          border: none;
          border-radius: var(--radius-md);
          background: var(--accent-cyan);
          color: #ffffff;
          font-size: 14px;
          font-weight: 600;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          gap: 8px;
          cursor: pointer;
          transition: background 0.15s ease, transform 0.1s ease;
        }

        .submit-button:hover:not(:disabled) {
          background: #0e9270;
        }

        .submit-button:active:not(:disabled) {
          transform: translateY(1px);
        }

        .submit-button:disabled {
          opacity: 0.6;
          cursor: wait;
        }

        .submit-button__spinner {
          width: 14px;
          height: 14px;
          border-radius: 50%;
          border: 2px solid rgba(255, 255, 255, 0.3);
          border-top-color: #ffffff;
          animation: login-spin 0.8s linear infinite;
        }

        @keyframes login-spin {
          to { transform: rotate(360deg); }
        }

        /* 左下角版本 / 环境 */
        .login-footer {
          position: absolute;
          bottom: 16px;
          left: 24px;
          z-index: 1;
          font-size: 10px;
          font-weight: 600;
          letter-spacing: 0.06em;
          text-transform: uppercase;
          color: var(--text-muted);
          user-select: none;
        }

        .login-footer__sep {
          margin: 0 2px;
        }
      `}</style>
    </main>
  </div>
);
```

补充实现说明：
- shake 动画的实现选了"在 .login-column 上 key={shakeNonce}"——这样每次错误把整列重挂，就同时触发 in + shake 两个 animation；in 用了 ease-out 8px 上飘，shake 用了 ease 4px 横移。两者并存对视觉无干扰：上飘是 400ms 完成的入场，shake 是 200ms 的水平抖动。如果 executor 觉得"出错时整列闪一下不优雅"，可以把入场动画收回到只在 first mount 触发（用 useState(true) + 第一次后置 false），但 MVP 实现先用 key 抖即可。
- 不要再渲染原来的 `.toggle-line` checkbox（已被眼睛图标替代）。
- 不要再保留旧的 `.login-panel`、`.login-form .field input` 旧 inline css —— 整段 <style>{...} 全部替换为新的。
- 不要修改 useShellStore() 的调用（即使现在没显式用到，原文件里它被引入应保留以避免 import 副作用回归）。如果 useShellStore 已经从原文件移除则不需要恢复。
- desktop config 路径：从 desktop/src/renderer/pages/LoginPage.tsx 出发，desktop/config/index.ts 的相对路径是 ../../../../config（pages -> renderer -> src -> desktop -> config）。请用 import 后立刻 typecheck（pnpm typecheck）确认；如果不通就调整层级。
- 错误信息 errorMessage 状态对象类型仍是 string；不要变 union。
- 整体结构必须确保 1440×900 视口下，单列在视觉中心，cyan 光晕在 logo 上方略微偏上的位置。

UI Style Guide 例外说明（写到 SUMMARY 里）：本页是 chrome-level 入口（应用启动后第一屏），按 ui-style-guide 第 555 行 "❌ 实心填充 CTA" 规则原本不允许实心 cyan 按钮——但登录主操作具有 OS 级权重（Linear / Raycast / 1Password 全部使用实心按钮），因此本页对该规则做 deliberate exception。其他页面继续遵守描边规则。
  </action>
  <verify>
    <automated>cd desktop && pnpm typecheck</automated>
    并人工确认：启动 `pnpm dev`，访问登录页，肉眼检查：(1) cyan 光晕在 logo 上方居中可见；(2) 三层文字垂直堆叠 + 居中对齐；(3) 密码字段右侧眼睛图标可点切换；(4) 输入错账号点登录后，错误条横向抖动 + AlertCircle 图标显示；(5) 左下角显示 "V0.1.0 · DEV"（dev 环境）；(6) 主按钮 hover 颜色变深、loading 时变 spinner + "正在登录..."；(7) TitleBar 仍然渲染在顶部不变；(8) data-testid 通过 DevTools 在 DOM 中可定位（共 5 个）。
  </verify>
  <done>
    desktop/src/renderer/pages/LoginPage.tsx 重写完成且 `pnpm typecheck` 通过；视觉上呈现 Linear 风极简单列；auth.login 签名 / redirectTarget / 错误码映射 / [desktop-login] 日志全部保留；5 个 data-testid 完整保留；不引入新依赖；不动 TitleBar / auth.ts / global.css。
  </done>
</task>

</tasks>

<verification>
单文件视觉重做，无后端 / 协议 / 状态层改动。整体验证以 typecheck + 人眼为准：

- [ ] `cd desktop && pnpm typecheck` 通过（main + renderer 两个 tsconfig 均无报错）
- [ ] 登录页打开后呈现单列 logo + 三层文字 + 表单 + CTA 的 Linear 风布局；无 .login-panel 卡片轮廓
- [ ] cyan 光晕位于 logo 上方略偏上，filter blur 有效
- [ ] 眼睛图标切换密码 type="password" / "text"
- [ ] 错误时 .login-column 发生 4px 抖动一次（200ms 内）
- [ ] 左下角显示 "V0.1.0 · {DEV|PRE|PROD}"
- [ ] data-testid="desktop-login-view|account|password|submit|error" 五个全部仍可被 querySelector 命中
- [ ] auth.login 仍旧以 `{ account, password }` 调用且 redirect 行为不变（手动登录一次走通）
- [ ] TitleBar 在顶部仍然渲染（`data-testid="custom-titlebar"` 仍存在）
</verification>

<success_criteria>
- LoginPage.tsx 重写后 `pnpm typecheck` 0 error
- 桌面登录页首屏无卡片边框、有 cyan 径向光晕、单列 400px 居中
- 密码可见性切换从 checkbox 迁移到 lucide Eye/EyeOff icon-button
- 错误展示从底部"红字一行"升级为顶部 banner（带 AlertCircle + 整列抖动）
- 左下角带版本+环境角标
- 登录核心行为零回归：调用、跳转、日志、redirect 解析、5 个 data-testid 不变
</success_criteria>

<output>
完成后创建 `.planning/quick/260508-hh3-linear/260508-hh3-SUMMARY.md`，记录：
1. 实际改了哪些行（可贴最关键的 diff 摘要）
2. shake 动画最终采用的实现方式（key 重挂 / className 切换）
3. desktop/config 的实际相对路径（../../../config 还是 ../../../../config）
4. UI Style Guide 例外说明（实心 CTA 在登录页的 deliberate exception）
5. typecheck 输出截图或文字（"0 errors"）
6. 是否触及任何测试（应为 0；否则说明原因）
</output>
