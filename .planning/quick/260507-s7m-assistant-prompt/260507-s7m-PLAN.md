---
phase: quick-260507-s7m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - desktop/shared/contracts/time-orchestration.ts
  - desktop/src/main/services/time-orchestration-store.ts
  - desktop/src/main/services/time-scheduler.ts
  - desktop/src/main/services/time-job-executor.ts
  - desktop/src/main/index.ts
  - desktop/src/renderer/stores/workspace.ts
  - desktop/src/renderer/pages/TimeCenterPage.tsx
autonomous: true
requirements:
  - S7M-01  # assistant_prompt 执行器接入 callModel 并把摘要落进 ExecutionRun
  - S7M-02  # 计划任务执行历史右侧抽屉（桌面原生侧滑面板）
  - S7M-03  # 三处按钮配色 / pill 圆角与 ui-style-guide 对齐

must_haves:
  truths:
    - "assistant_prompt 类型计划任务到期后会真的调用默认模型，并把模型回复写进对应 ExecutionRun 的 outputSummary 字段"
    - "在 ScheduleJobListPage 行卡上点击（避开操作按钮）会从右侧滑出执行历史抽屉，列出该 job 的全部 ExecutionRun（按 startedAt 倒序，含状态徽章 / 时间范围 / outputSummary 全文 / errorMessage 全文）"
    - "抽屉打开时会主动刷新一次 executionRuns，确保最新一次执行结果能立即看到"
    - "TimeCenterPage 内 .composer-tab-btn 圆角、.schedule-summary-bar__feedback / .summary-chip.is-warning 色值、删除按钮 danger 配色全部使用 ui-style-guide 的 token（var(--radius-md) / var(--accent-cyan) / var(--status-red)）"
  artifacts:
    - path: "desktop/shared/contracts/time-orchestration.ts"
      provides: "ExecutionRun 契约扩展为 entityKind/entityId/jobId? 三栏存在；ExecutionRunStatus 与 scheduler 写入 status 之间的映射明确"
      contains: "ExecutionRun"
    - path: "desktop/src/main/services/time-job-executor.ts"
      provides: "assistant_prompt 分支调用 deps.runAssistantPrompt 并返回 { outputSummary?: string }"
      contains: "runAssistantPrompt"
    - path: "desktop/src/main/services/time-scheduler.ts"
      provides: "TimeExecutionRunInput 增加 outputSummary 字段，runSingleScheduleJob 把执行器返回值写入 ExecutionRun"
      contains: "outputSummary"
    - path: "desktop/src/main/index.ts"
      provides: "createTimeJobExecutor 注入 runAssistantPrompt，内部走 callModel(defaultProfile)"
      contains: "runAssistantPrompt"
    - path: "desktop/src/renderer/stores/workspace.ts"
      provides: "WorkspaceTimeState.refreshExecutionRuns action（既新增 IPC 调用入口，也供抽屉打开时调用）"
      contains: "refreshExecutionRuns"
    - path: "desktop/src/renderer/pages/TimeCenterPage.tsx"
      provides: "ExecutionHistoryDrawer 组件 + ScheduleJobListPage 行点击触发逻辑 + ActionIconButton variant=danger + 三处 token 化样式修复"
      contains: "ExecutionHistoryDrawer"
  key_links:
    - from: "desktop/src/main/services/time-scheduler.ts"
      to: "desktop/src/main/services/time-job-executor.ts"
      via: "deps.runScheduleJob 返回值（{outputSummary?}）→ recordExecutionRun(outputSummary)"
      pattern: "outputSummary"
    - from: "desktop/src/main/services/time-job-executor.ts"
      to: "desktop/src/main/services/model-client.ts"
      via: "deps.runAssistantPrompt → callModel(defaultProfile, [{role:'user', content: prompt}])"
      pattern: "callModel"
    - from: "desktop/src/renderer/pages/TimeCenterPage.tsx"
      to: "desktop/src/renderer/stores/workspace.ts"
      via: "drawer onMount → workspace.refreshExecutionRuns()"
      pattern: "refreshExecutionRuns"
    - from: "desktop/src/renderer/pages/TimeCenterPage.tsx ScheduleJobListPage"
      to: "ExecutionHistoryDrawer"
      via: "selectedJobForHistory state + onClick on article (非 .job-row__actions 容器内)"
      pattern: "selectedJobForHistory"
---

<objective>
修复时间规划定时任务三处缺口：
1. assistant_prompt 执行器接入 callModel，把模型回复写进 ExecutionRun 让用户看见结果。
2. ScheduleJobListPage 加执行历史右侧抽屉（桌面原生侧滑，不是新路由），让用户点行卡即可看任意一次跑批的完整 outputSummary / errorMessage。
3. TimeCenterPage 三处不符合 ui-style-guide 的硬编码色值与圆角，统一改为 token。

Purpose: assistant_prompt 当前是 no-op，用户排了任务但什么都没发生；执行结果只能看到 "成功/失败" 状态徽章，看不到模型实际输出；TimeCenterPage 在 ui-style-guide 下还有 999px pill / 硬编码红绿等违规残留。
Output: 一次提交三个原子改动（按 task 拆分），最终统一 typecheck。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@desktop/docs/ui-style-guide.md
@desktop/src/main/services/time-job-executor.ts
@desktop/src/main/services/time-scheduler.ts
@desktop/src/main/index.ts
@desktop/src/main/services/runtime-context.ts
@desktop/src/main/services/model-client.ts
@desktop/src/main/ipc/time-orchestration.ts
@desktop/src/preload/index.ts
@desktop/src/renderer/types/electron.d.ts
@desktop/src/renderer/stores/workspace.ts
@desktop/src/renderer/pages/TimeCenterPage.tsx
@desktop/shared/contracts/time-orchestration.ts
@desktop/src/main/services/time-orchestration-store.ts
@desktop/src/renderer/styles/global.css

<interfaces>
<!-- 关键契约：ExecutionRun 现在的形状与持久化写入形状不一致 -->

contracts/time-orchestration.ts (line 84-101):
```typescript
export type ExecutionRunStatus = "running" | "succeeded" | "failed" | "cancelled";
export type ExecutionRun = {
  id: string;
  jobId: string;            // ⚠️ 渲染层期望
  status: ExecutionRunStatus;
  startedAt: string;
  finishedAt?: string;
  outputSummary?: string;
  errorMessage?: string;
};
```

services/time-scheduler.ts (line 8-15) — 当前传给 recordExecutionRun 的形状：
```typescript
export type TimeExecutionRunInput = {
  entityKind: "reminder" | "schedule_job";
  entityId: string;          // ⚠️ 实际写入的字段
  status: "completed" | "failed";  // ⚠️ 与 ExecutionRunStatus 不重叠
  startedAt: string;
  finishedAt: string;
  note?: string;
};
```

services/time-orchestration-store.ts (line 44-46, 514-535):
```typescript
function parseExecutionRun(row): ExecutionRun {
  return JSON.parse(String(row.payload_json)) as ExecutionRun;  // ⚠️ 类型断言谎报
}
async recordExecutionRun(input: ExecutionRunRecordInput): Promise<void> {
  // payload_json: JSON.stringify(input)  ← 写入 entityKind/entityId/note，不是 jobId/outputSummary
}
```

services/model-client.ts (line 462):
```typescript
export async function callModel(options: ModelCallOptions): Promise<ModelCallResult>;
// ModelCallResult 含 .content: string
```

services/time-job-executor.ts (line 7-10) — 现有依赖形状：
```typescript
export type TimeJobExecutorDeps = {
  startWorkflowRun: (input: { workflowId: string; siliconPersonId?: string }) => Promise<void>;
  sendSiliconPersonMessage: (input: { siliconPersonId: string; content: string }) => Promise<void>;
};
```

stores/workspace.ts (line 131, 770):
```typescript
executionRuns: ExecutionRun[];
// 当前唯一刷新时机：executeScheduleJobNow 之后；bootstrap/初始挂载不主动拉。
```

global.css 已确认存在的 token：
- --status-red: #ef4444
- --accent-cyan: #10a37f
- --bg-drawer: #161b22
- --shadow-drawer: -12px 0 32px rgba(0, 0, 0, 0.5)
- --radius-md: 6px (注意：style guide 里 doc 写 6px，但项目里历史是 7px — Task 3 验证后取实际值)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: assistant_prompt 执行器落地，把 outputSummary 串到 ExecutionRun</name>
  <files>
    desktop/shared/contracts/time-orchestration.ts,
    desktop/src/main/services/time-orchestration-store.ts,
    desktop/src/main/services/time-scheduler.ts,
    desktop/src/main/services/time-job-executor.ts,
    desktop/src/main/index.ts
  </files>
  <action>
    把 assistant_prompt 从 no-op 升级到真正调 callModel，并打通 outputSummary → ExecutionRun 的整条链。

    分四步改：

    (1) `desktop/shared/contracts/time-orchestration.ts`
        ExecutionRun 已有 jobId / outputSummary / errorMessage 字段无需新增；但 ExecutionRunStatus 是 `running | succeeded | failed | cancelled`，与 scheduler 当前写入的 `completed | failed` 不一致。
        在 file 末尾新增一个内部映射常量 + 备注（不导出新枚举，保持契约最小侵入）：
        ```ts
        // scheduler→contract 状态映射：内部 completed 对应对外 succeeded（与 ExecutionRunStatus 对齐）
        ```
        实际状态转换在 store / scheduler 里做，契约层不变更字面量。

    (2) `desktop/src/main/services/time-orchestration-store.ts`
        - `ExecutionRunRecordInput` 增加可选字段 `jobId?: string`、`outputSummary?: string`、`errorMessage?: string`。保留 `entityKind/entityId/note` 不动，向后兼容。
        - `recordExecutionRun` 改为写入符合 ExecutionRun 契约的 payload，让 parseExecutionRun 不再类型谎报：
          ```ts
          const persistedRun: ExecutionRun = {
            id: randomUUID(),
            jobId: input.jobId ?? input.entityId,    // 兜底：reminder 也走 entityId
            status: input.status === "completed" ? "succeeded" : "failed",
            startedAt: input.startedAt,
            finishedAt: input.finishedAt,
            outputSummary: input.outputSummary,
            errorMessage: input.errorMessage ?? input.note,
          };
          // 然后 payload_json = JSON.stringify(persistedRun);
          // INSERT 时 id 用 persistedRun.id；status 列用 input.status（保持表的 completed/failed 旧值，避免 schema migration）
          ```
          目的：DB 字段不动，但 payload_json 内容现在与 ExecutionRun 契约一致，渲染层 store 拿到的 executionRuns 真的有 jobId / outputSummary / errorMessage。

    (3) `desktop/src/main/services/time-scheduler.ts`
        - `TimeExecutionRunInput` 类型增加可选 `outputSummary?: string`、`errorMessage?: string`、`jobId?: string`。
        - `runScheduleJob?` 签名改为 `(job: DueScheduleJob) => Promise<{ outputSummary?: string } | void>`（保持 void 兼容，executor 不返回时不报错）。
        - `runSingleScheduleJob` 内：
          ```ts
          let succeeded = true;
          let failureNote: string | undefined;
          let outputSummary: string | undefined;
          try {
            const result = await deps.runScheduleJob(job);
            outputSummary = result?.outputSummary;
          } catch (error) {
            succeeded = false;
            failureNote = error instanceof Error ? error.message : String(error);
          }
          // ...
          await deps.recordExecutionRun({
            entityKind: "schedule_job",
            entityId: job.id,
            jobId: job.id,
            status: succeeded ? "completed" : "failed",
            startedAt,
            finishedAt,
            note: failureNote,
            outputSummary,
            errorMessage: failureNote,
          });
          ```
        - reminder 分支（line ~248-262）也补 `jobId: reminder.id`，让 ExecutionRun.jobId 总有值。

    (4) `desktop/src/main/services/time-job-executor.ts`
        - `TimeJobExecutorDeps` 增加：
          ```ts
          runAssistantPrompt: (input: { prompt: string }) => Promise<{ outputSummary: string }>;
          ```
        - `execute` 返回类型改为 `Promise<{ outputSummary?: string } | void>`。
        - 把现有 workflow / silicon_person 分支保持不变（它们没有 outputSummary，return undefined / void 即可）。
        - assistant_prompt 分支替换为：
          ```ts
          case "assistant_prompt": {
            const prompt = (job.description ?? job.title).trim();
            if (!prompt) {
              throw new Error("assistant_prompt 类型计划任务缺少 prompt 内容（description/title 都为空）");
            }
            const { outputSummary } = await deps.runAssistantPrompt({ prompt });
            const truncated = outputSummary.length > 500
              ? outputSummary.slice(0, 500) + "…"
              : outputSummary;
            logger.info("计划任务 assistant_prompt 已生成摘要", {
              jobId: job.id,
              promptLength: prompt.length,
              outputLength: outputSummary.length,
            });
            return { outputSummary: truncated };
          }
          default:
            logger.warn("计划任务命中未知 executor 类型，跳过执行", { jobId: job.id, executor: job.executor });
            return;
          ```
          注意：default 分支独立出来不要再 fallthrough，避免 assistant_prompt 写完后 default 也跑一次空 log。

    (5) `desktop/src/main/index.ts` (line ~189-220)
        在 `createTimeJobExecutor({ ... })` wiring 处新增 `runAssistantPrompt`：
        ```ts
        runAssistantPrompt: async ({ prompt }) => {
          const profileId = ctx?.state.getDefaultModelProfileId() ?? defaultModelProfileId;
          const profile = profileId ? models.find((m) => m.id === profileId) : null;
          if (!profile) {
            throw new Error("未配置默认模型，assistant_prompt 计划任务无法执行");
          }
          const result = await callModel({
            profile,
            messages: [{ role: "user", content: prompt }],
            timeoutMs: 60_000,
          });
          return { outputSummary: result.content ?? "" };
        },
        ```
        注意：ctx 在此处可能尚未就绪（createTimeJobExecutor 在 ctx 之前构造），所以同时回退到闭包里的 `models` + `defaultModelProfileId`，与 MeetingRecorder 里 `targetId = asrConfig.summaryModelProfileId ?? defaultModelProfileId` (line 251) 同样的兜底姿势。

    最后：scheduler 里 `await deps.runScheduleJob(job)` 是 void 调用，要改成接收返回值并往下传：
    ```ts
    const result = await deps.runScheduleJob(job);
    outputSummary = result?.outputSummary;
    ```
    （已在第 3 步说明，核对 main/index.ts 注入处 `runScheduleJob: async (job) => { return await timeJobExecutor.execute(job); }` 也要 return。）

    禁止：不要新增 IPC、不要改 cloud、不要做 DB schema migration（execution_runs 表的 status 列继续保留 completed/failed 值，对外契约通过 payload_json 转换）。
  </action>
  <verify>
    <automated>由最终统一 typecheck 保证（pnpm -C desktop typecheck）</automated>
  </verify>
  <done>
    - assistant_prompt 类型 ScheduleJob 触发后会真的调用默认模型，且模型 .content 写入对应 ExecutionRun.outputSummary（截断到 500 字符）。
    - 渲染层从 store 拿到的 ExecutionRun 含正确 jobId / status (succeeded|failed) / outputSummary / errorMessage 字段（不再是断言谎报）。
    - workflow / silicon_person 两个原有分支行为不变。
    - 没有新增 IPC，没改数据库 schema。
  </done>
</task>

<task type="auto">
  <name>Task 2: ScheduleJobListPage 接入执行历史右侧抽屉（桌面原生侧滑，不是新页面）</name>
  <files>
    desktop/src/renderer/stores/workspace.ts,
    desktop/src/renderer/pages/TimeCenterPage.tsx
  </files>
  <action>
    给定时任务列表加"点行卡 → 看历史"的桌面原生交互。

    (1) `desktop/src/renderer/stores/workspace.ts`
        在 actions 区新增 `refreshExecutionRuns`：
        ```ts
        async refreshExecutionRuns() {
          const { items: executionRuns } = await window.myClawAPI.time.listExecutionRuns();
          set((state) => ({
            time: { ...state.time, executionRuns },
          }));
        },
        ```
        以及在 WorkspaceState 类型 / 接口里把它声明出来（参照同区 `executeScheduleJobNow` / `refreshTodayBrief` 的写法）。
        bootstrap 里如果已有 time 域初始化（搜 `listScheduleJobs` 周边），并行追加 `await window.myClawAPI.time.listExecutionRuns()` 让首次进入页面就有数据；如果当前 bootstrap 没拉 executionRuns，就保持现状不强加，依赖抽屉打开时主动拉一次即可。

    (2) `desktop/src/renderer/pages/TimeCenterPage.tsx` — 抽屉组件
        在 `ScheduleJobListPage` 函数定义之上（约 line 654 之前）新增：
        ```tsx
        function ExecutionHistoryDrawer({
          job,
          runs,
          onClose,
        }: {
          job: ScheduleJob;
          runs: ExecutionRun[];
          onClose: () => void;
        }): React.JSX.Element {
          const sortedRuns = useMemo(
            () => [...runs].filter((r) => r.jobId === job.id).sort((a, b) => b.startedAt.localeCompare(a.startedAt)),
            [runs, job.id],
          );
          // Esc 关闭
          useEffect(() => {
            const handler = (e: KeyboardEvent) => { if (e.key === "Escape") onClose(); };
            window.addEventListener("keydown", handler);
            return () => window.removeEventListener("keydown", handler);
          }, [onClose]);
          return (
            <div className="execution-history-drawer-overlay" onClick={onClose}>
              <aside
                className="execution-history-drawer"
                role="dialog"
                aria-label={`计划任务 ${job.title} 的执行历史`}
                onClick={(e) => e.stopPropagation()}
              >
                <header className="execution-history-drawer__header">
                  <div>
                    <span className="execution-history-drawer__eyebrow">执行历史</span>
                    <h3 className="execution-history-drawer__title">{job.title}</h3>
                  </div>
                  <button type="button" className="job-action-icon-btn" onClick={onClose} aria-label="关闭 (Esc)">
                    <IconClose />
                  </button>
                </header>
                <div className="execution-history-drawer__body">
                  {sortedRuns.length === 0 ? (
                    <div className="execution-history-drawer__empty">该任务尚无执行记录</div>
                  ) : (
                    <ul className="execution-history-list">
                      {sortedRuns.map((run) => (
                        <li key={run.id} className="execution-history-row">
                          <div className="execution-history-row__head">
                            <span className={`status-badge status-badge--${run.status === "succeeded" ? "active" : run.status === "failed" ? "danger" : "muted"}`}>
                              {formatRunStatus(run.status)}
                            </span>
                            <span className="execution-history-row__time">
                              {formatDateTime(run.startedAt, job.timezone)}
                              {run.finishedAt ? ` → ${formatDateTime(run.finishedAt, job.timezone)}` : ""}
                            </span>
                          </div>
                          {run.outputSummary ? (
                            <pre className="execution-history-row__output">{run.outputSummary}</pre>
                          ) : null}
                          {run.errorMessage ? (
                            <pre className="execution-history-row__error">{run.errorMessage}</pre>
                          ) : null}
                        </li>
                      ))}
                    </ul>
                  )}
                </div>
              </aside>
            </div>
          );
        }
        ```
        新增辅助函数 `formatRunStatus(status: ExecutionRun["status"])` 返回中文（"已成功"/"已失败"/"运行中"/"已取消"）。
        新增 `IconClose` SVG（与同文件 `IconPlay`/`IconRestore` 同风格 14×14 stroke）。

    (3) `ScheduleJobListPage` 行卡接入触发器（line 682-709 区域）
        - 在 `ScheduleJobListPage` 内部用 useState：
          ```tsx
          const [selectedJobForHistory, setSelectedJobForHistory] = useState<ScheduleJob | null>(null);
          ```
        - article 加 `role="button" tabIndex={0} onClick={() => setSelectedJobForHistory(job)} onKeyDown={(e) => { if (e.key === "Enter") setSelectedJobForHistory(job); }}`。
        - `<div className="job-row__actions">` 容器加 `onClick={(e) => e.stopPropagation()}`，避免点击操作按钮误触发抽屉。
        - 列表渲染末尾追加：
          ```tsx
          {selectedJobForHistory ? (
            <ExecutionHistoryDrawer
              job={selectedJobForHistory}
              runs={executionRuns}
              onClose={() => setSelectedJobForHistory(null)}
            />
          ) : null}
          ```
          executionRuns 从 props 接收（确认 ScheduleJobListPage 现有 props 是否含 executionRuns；若无则在 props 里加）。
        - 抽屉打开时刷一次：在 ScheduleJobListPage 里加：
          ```tsx
          useEffect(() => {
            if (selectedJobForHistory) {
              void useWorkspaceStore.getState().refreshExecutionRuns();
            }
          }, [selectedJobForHistory]);
          ```
          （如 ScheduleJobListPage 通过 props 注入 store，则改成 props 传入的 refresh 函数。）

    (4) 同文件 styles 模板字符串末尾追加抽屉样式（与现有 `.list-page-row` 同区，搜索现有 `.list-page-body` 样式块附近）：
        ```css
        .execution-history-drawer-overlay {
          position: fixed;
          inset: 0;
          background: rgba(0, 0, 0, 0.6);
          backdrop-filter: blur(4px);
          z-index: 100;
          display: flex;
          justify-content: flex-end;
          animation: fadeIn 0.2s ease;
        }
        .execution-history-drawer {
          width: 480px;
          max-width: 100%;
          height: 100%;
          background: var(--bg-drawer);
          border-left: 1px solid var(--glass-border);
          box-shadow: var(--shadow-drawer);
          display: flex;
          flex-direction: column;
          animation: slideIn 0.3s cubic-bezier(0.16, 1, 0.3, 1);
        }
        .execution-history-drawer__header {
          display: flex;
          align-items: flex-start;
          justify-content: space-between;
          padding: 20px 24px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.06);
        }
        .execution-history-drawer__eyebrow {
          font-size: 11px;
          font-weight: 700;
          letter-spacing: 0.08em;
          text-transform: uppercase;
          color: var(--text-muted);
        }
        .execution-history-drawer__title {
          margin: 4px 0 0;
          font-size: 16px;
          font-weight: 600;
          color: var(--text-primary);
        }
        .execution-history-drawer__body {
          flex: 1;
          overflow-y: auto;
          padding: 16px 20px;
        }
        .execution-history-drawer__empty {
          padding: 48px 16px;
          text-align: center;
          color: var(--text-muted);
          font-size: 13px;
        }
        .execution-history-list {
          list-style: none;
          margin: 0;
          padding: 0;
          display: flex;
          flex-direction: column;
          gap: 12px;
        }
        .execution-history-row {
          padding: 14px 16px;
          background: var(--bg-surface);
          border: 1px solid var(--glass-border);
          border-radius: var(--radius-md);
          display: flex;
          flex-direction: column;
          gap: 8px;
        }
        .execution-history-row__head {
          display: flex;
          align-items: center;
          gap: 10px;
        }
        .execution-history-row__time {
          font-size: 12px;
          color: var(--text-muted);
        }
        .execution-history-row__output {
          margin: 0;
          padding: 10px 12px;
          background: rgba(255, 255, 255, 0.03);
          border-radius: var(--radius-md);
          color: var(--text-primary);
          font-size: 12px;
          line-height: 1.6;
          white-space: pre-wrap;
          word-break: break-word;
          font-family: inherit;
        }
        .execution-history-row__error {
          margin: 0;
          padding: 10px 12px;
          background: rgba(239, 68, 68, 0.08);
          border: 1px solid rgba(239, 68, 68, 0.2);
          border-radius: var(--radius-md);
          color: var(--status-red);
          font-size: 12px;
          line-height: 1.6;
          white-space: pre-wrap;
          word-break: break-word;
          font-family: inherit;
        }
        @keyframes fadeIn { from { opacity: 0 } to { opacity: 1 } }
        @keyframes slideIn { from { transform: translateX(100%) } to { transform: translateX(0) } }

        /* status-badge danger 变体（如 global.css 还没有 --danger，则在此就地补一份本页用） */
        .status-badge--danger {
          background: rgba(239, 68, 68, 0.12);
          border: 1px solid rgba(239, 68, 68, 0.28);
          color: var(--status-red);
        }
        ```

    (5) ScheduleJobListPage 调用方（line ~317）确认 executionRuns 已经作为 prop 传入；若没有，从 useWorkspaceStore 顶层 selector 取出：
        ```tsx
        const executionRuns = useWorkspaceStore((s) => s.time.executionRuns);
        ```
        然后透传给 `<ScheduleJobListPage executionRuns={executionRuns} ... />`。

    禁止：不要新加 IPC（已经有 time:list-execution-runs）；不要做新路由；抽屉不许是 modal 居中样式。
  </action>
  <verify>
    <automated>由最终统一 typecheck 保证（pnpm -C desktop typecheck）</automated>
  </verify>
  <done>
    - 在定时任务列表点任意一行（避开"执行/暂停/删除"按钮）会从右侧滑出 480px 宽抽屉。
    - 抽屉首次打开时会调用 workspace.refreshExecutionRuns() 刷新一次，再渲染。
    - 列表按 startedAt 倒序，每行：状态徽章 + 时间范围 + outputSummary（pre-wrap 全文）+ errorMessage（红字 pre-wrap 全文）。
    - 点遮罩 / Esc / 右上角 × 都能关闭。
    - 不存在 executionRuns 时显示"该任务尚无执行记录"。
    - 没有新加 IPC、没有新路由。
  </done>
</task>

<task type="auto">
  <name>Task 3: TimeCenterPage 三处按钮配色与圆角对齐 ui-style-guide</name>
  <files>
    desktop/src/renderer/pages/TimeCenterPage.tsx
  </files>
  <action>
    全部在 `desktop/src/renderer/pages/TimeCenterPage.tsx` 的 styles 模板字符串与 ActionIconButton 组件内修改。改动顺序：

    (1) `.composer-tab-btn` (line ~1668-1671)
        当前：`border-radius: 999px;`（pill 圆形）— 这是 ui-style-guide 明确禁止的"行内 pill"。
        改为：`border-radius: var(--radius-md);` 与同文件 .planning-view-tab 一致，保持 tab 的矩形描边风格。
        额外：保留现有 hover / is-active 颜色不动。

    (2) `.schedule-summary-bar__feedback` (line ~1359-1363)
        当前：`color: #b6f3df;`（硬编码浅绿）。
        改为：`color: var(--accent-cyan);`（与 ui-style-guide 主强调色一致）。

    (3) `.summary-chip.is-warning` (line ~1348-1352)
        当前：
        ```css
        color: #ffd1d1;
        border-color: rgba(239, 68, 68, 0.28);
        background: rgba(239, 68, 68, 0.08);
        ```
        改为：
        ```css
        color: var(--status-red);
        border-color: rgba(239, 68, 68, 0.28);
        background: rgba(239, 68, 68, 0.08);
        ```
        保持 rgba 的 border/background（global.css 没有 status-red 的 alpha 变体 token，rgba 直接用 #ef4444 的对应通道是当前项目惯例）。
        在该 css 块上方加一行注释："/* 红色仅用 var(--status-red) 取文字色；border/background 暂沿用 rgba 等价表达，待全局 alpha token 落地再统一 */"。

    (4) ActionIconButton 加 variant=danger 支持 (line 359-374)
        - 把 props 类型改为：
          ```ts
          {
            title: string;
            onClick: () => void | Promise<void>;
            variant?: "default" | "danger";
            children: React.ReactNode;
          }
          ```
        - 默认 `variant = "default"`。
        - className 改为 ``${variant === "danger" ? "job-action-icon-btn job-action-icon-btn--danger" : "job-action-icon-btn"}``。
        - 删除按钮调用处 (line 705-707) 改为 `<ActionIconButton title="删除" variant="danger" onClick={() => onDelete(job.id)}>`。
        - styles 模板字符串内 `.job-action-icon-btn` 块下方新增：
          ```css
          .job-action-icon-btn--danger {
            color: var(--status-red);
            border-color: rgba(239, 68, 68, 0.32);
          }
          .job-action-icon-btn--danger:hover {
            background: rgba(239, 68, 68, 0.10);
            color: var(--status-red);
            border-color: rgba(239, 68, 68, 0.55);
          }
          ```

    (5) 自查 ui-style-guide checklist（不写代码，做完后人眼对照）：
        - 描边按钮 ✓
        - var(--radius-md) ✓
        - var(--accent-cyan) / var(--status-red) token ✓
        - 不引入新的 emoji / 渐变 / scale 动画 ✓

    禁止：不要顺手改 .planning-view-tab、.list-page-row 等无关样式块；不要把 .summary-chip rgba 边框改成不存在的 token（如 var(--status-red-alpha-28)）；不要把 .composer-tab-btn 改成 .btn-primary 之类的全局类（保留页面内 scope）。
  </action>
  <verify>
    <automated>由最终统一 typecheck 保证（pnpm -C desktop typecheck）</automated>
  </verify>
  <done>
    - .composer-tab-btn 圆角是 var(--radius-md)，不再是 999px。
    - .schedule-summary-bar__feedback 文字色是 var(--accent-cyan)，不再硬编码 #b6f3df。
    - .summary-chip.is-warning 文字色是 var(--status-red)，不再硬编码 #ffd1d1。
    - ScheduleJobListPage 行内"删除"按钮显示为 status-red 描边色，hover 时呈现微红背景。
    - 同文件其他样式块未被顺手修改。
  </done>
</task>

</tasks>

<verification>
- 三个 task 改完后统一跑一次：`pnpm -C desktop typecheck`（per memory：跳过中途测试，最后一起跑）。
- 启动 desktop dev 手动 smoke：
  1. 创建一个 assistant_prompt 计划任务（schedule_kind=once，nextRunAt=1分钟后），等到期，确认列表里 latestRun 状态变 succeeded 且点击该行抽屉里能看到模型实际回复文本。
  2. 创建一个故意失败的（删默认模型 / prompt 为空），等执行后确认抽屉里能看到 errorMessage 红字全文。
  3. 视觉对照 ui-style-guide：composer tab 矩形圆角、summary feedback 用 cyan、warning chip 用 red token、删除按钮微红 hover。
</verification>

<success_criteria>
- [ ] assistant_prompt 计划任务能真的跑模型并把内容写进 ExecutionRun.outputSummary
- [ ] 渲染层 ExecutionRun 真的有 jobId / outputSummary / errorMessage 字段（payload_json 与契约对齐）
- [ ] ScheduleJobListPage 行卡可点击，从右滑出执行历史抽屉，列出全部 runs
- [ ] 抽屉打开时主动 refreshExecutionRuns 一次
- [ ] 抽屉支持 Esc / 遮罩 / × 三种关闭路径，操作按钮 stopPropagation 不误触发
- [ ] .composer-tab-btn 圆角、.schedule-summary-bar__feedback / .summary-chip.is-warning 色值、删除按钮 danger 样式全部 token 化
- [ ] 最终 `pnpm -C desktop typecheck` 通过（无中途单 task 验证）
</success_criteria>

<output>
完成后写一份 STATE.md 更新（在最后整体 commit 之前），记录本次 quick task 完成、commit hash、关键决策（assistant_prompt 接 callModel + ExecutionRun payload reshape + 抽屉无新 IPC）。
</output>
