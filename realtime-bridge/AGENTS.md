# realtime-bridge AGENTS.md

## 作用范围

- 本文件适用于 `realtime-bridge/` 后端服务。
- 上层 `AGENTS.md` 的 UTF-8、TDD、中文注释与中文日志规则仍然生效。

## 本地硬规则

- 所有文件必须使用 UTF-8 保存，严禁提交中文乱码。
- 所有方法必须写中文注释，方法内关键路径必须写中文日志。
- 每个任务必须先写失败测试，再实现，再验证。
- 不允许直接导入 `desktop/` 内部实现；跨工作区共享只能通过明确契约。
